{
  "sourceOfTruth": "aidcp/docs/cloud-service-decomposition-proposal.md §4.7「归属总表」（含 §4.6.1–§4.6.8、§7.2 判据三的逐文件点名）。本文件是该表的机械转写，MUST NOT 在此另立判据；认为某行有误时走控制仓 change 改 §4.7 后再回写本文件。",
  "howToRegenerate": "npx tsx test/acceptance/helpers/boundary-record.ts ownership",
  "baseline": {
    "sha": "aidcp-cloud@3d12d21",
    "files": 321,
    "lines": 85591,
    "byLayer": { "api": 91, "content": 78, "automation": 146, "kernel": 4, "composition": 2 },
    "note": "§4.7 的基线口径。本仓 HEAD 通常已超出该 sha；目录规则负责接住新增文件，逐文件差额由 boundary-record 的对账输出给出。"
  },
  "seedWindow": {
    "open": true,
    "whyOpen": "门禁 change cloud-service-boundary-gates 尚未归档，棘轮尚未开始计数，seed 基线仍可随合并基线移动。",
    "closeWhen": "归档 cloud-service-boundary-gates 时把 open 改成 false。此后 boundary-record 的 --reseed 一律拒绝——棘轮已开始计数，重新 seed 等于把它拆掉。"
  },
  "adjudicatedFilesRoster": "boundaries/adjudicated-files.json —— 「逐文件切分目录里哪些文件已经被人判过」的人工名册，是 expandOwnership 的准入依据。MUST NOT 改用生成物 module-ownership.json 顶替：那会让判据退化成「是否已经在生成物里」，手工往生成物塞一条新文件 + 目录默认层就能全绿、refresh 随后永久洗白（2026-07-23 审计坐实）。",
  "newFileRule": "每条目录规则的 newFile 字段决定该目录**新增文件**的处置：inherit = §4.7 里该目录整行只有一个归属层，新文件的归属已被 §4.7 判定，目录规则可以接住；adjudicate = §4.7 把该目录逐文件切开，§4.7 并没有判定「该目录下任意新文件属于哪一层」，故新增文件 MUST 先在 fileOverrides 里逐个裁定，生成器在裁定前报错并列出清单（绝不塞默认层）。字段缺省按 adjudicate 处理。",
  "compositionWhitelist": ["src/server.ts", "src/index.ts"],
  "directoryRules": [
    { "dir": "src/agents/", "layer": "automation", "newFile": "adjudicate", "basis": "§4.7 src/agents/ 50 文件：43 automation 为 content 5 + api 2 的补集" },
    { "dir": "src/alerts/", "layer": "automation", "newFile": "inherit", "basis": "§4.7：alerts 表 owner（2/2 automation）" },
    { "dir": "src/api-contracts/", "layer": "api", "newFile": "inherit", "basis": "【待定稿裁决】§10.9：protocol.ts 归 automation 独占、MUST NOT 进 kernel，api 与 content MUST NOT 导入它（含仅类型导入）。本目录是 api 段自己的 contracts/，只放对边云协议载荷形状的**本地重抄**（纯类型、零 import、零运行时）；整目录只有 api 一个归属层，故 newFile=inherit。逐字同形由 test/acceptance/api-contract-drift.test.ts 在 typecheck 层钉住。请求回写 §4.7 目录行与计数，见 docpatch RC-P2-4" },
    { "dir": "src/cache/", "layer": "automation", "newFile": "adjudicate", "basis": "§4.7 src/cache/ 逐文件表的多数落点" },
    { "dir": "src/cli/", "layer": "automation", "newFile": "adjudicate", "basis": "§4.7 src/cli/ 逐文件表：运维脚本随其调用面走" },
    { "dir": "src/client-auth/", "layer": "api", "newFile": "inherit", "basis": "§4.7：客户身份与环境归属权威（7/7 api）" },
    { "dir": "src/comm/", "layer": "automation", "newFile": "inherit", "basis": "§4.7：Edge Gateway、协议、租约、验证码协助（15/15 automation）；protocol.ts 归属见 §10.9" },
    { "dir": "src/comment-agent/", "layer": "automation", "newFile": "inherit", "basis": "§4.7：命令式评论与加群调度（13/13 automation）" },
    { "dir": "src/config/", "layer": "api", "newFile": "adjudicate", "basis": "§4.6.8：25 文件归 api，5 个限频配置文件例外（另 3 个门面已在定稿 §4.6.8 判 automation，翻转与 P2-1 同批落，见控制仓耦合处置清单）" },
    { "dir": "src/delegated-task/", "layer": "automation", "newFile": "inherit", "basis": "§4.7：认领型 worker 范式（整目录 automation，无例外）。**判据是范式，不是当时的文件数**——原文写死「10/10」，目录长到 12 个之后那个数字就只会误导（新增的是 change split-cloud-automation-production-runtime 的运营指令接收方与幂等台账，同属该范式）" },
    { "dir": "src/event-bus/", "layer": "automation", "newFile": "inherit", "basis": "§4.7：进程内总线，不跨服务（2/2 automation）" },
    { "dir": "src/feishu/", "layer": "api", "newFile": "inherit", "basis": "§4.6.2 裁决：src/feishu/ 整体归 aidcp-api（15/15）" },
    { "dir": "src/gateway/", "layer": "api", "newFile": "inherit", "basis": "§4.7 基线 sha 之后由 change block2-data-gateway 新增的目录：api/panel 收口取数的数据网关，只聚合 kernel 读端口、只 import kernel 契约（不连 transport/automation），无 SQL、无进程内活状态。整目录归 api、新文件 inherit。MUST 回写 §4.7" },
    { "dir": "src/hot-lead/", "layer": "automation", "newFile": "inherit", "basis": "§4.7：浏览闭环内触发（2/2 automation）" },
    { "dir": "src/interactions/", "layer": "automation", "newFile": "adjudicate", "basis": "§4.6.1：收件箱 / 回复工作流 / 发送与回执 / 离场 saga 段" },
    { "dir": "src/llm/", "layer": "content", "newFile": "inherit", "basis": "§4.7：模型客户端（4/4 content）" },
    { "dir": "src/metrics/", "layer": "content", "newFile": "inherit", "basis": "§4.6.6：用量与成本两张表归 aidcp-content（2/2）" },
    { "dir": "src/onboarding/", "layer": "api", "newFile": "adjudicate", "basis": "§4.7：状态表归 api、创作桥接归 content" },
    { "dir": "src/orchestrator/", "layer": "automation", "newFile": "adjudicate", "basis": "§4.7：content-scheduler.ts 归 api，其余归 automation" },
    { "dir": "src/panel/", "layer": "api", "newFile": "inherit", "basis": "§4.6.4：全部 12 个文件归 aidcp-api" },
    { "dir": "src/planner/", "layer": "automation", "newFile": "inherit", "basis": "§4.7：v1 遗留路径（3/3 automation）" },
    { "dir": "src/platform/", "layer": "automation", "newFile": "inherit", "basis": "§4.7：平台能力注册表（§9 单写，4/4 automation）" },
    { "dir": "src/publish-agent/", "layer": "content", "newFile": "adjudicate", "basis": "§4.6.3：生成候选段，api 6 与 automation 6 逐文件点名" },
    { "dir": "src/render/", "layer": "content", "newFile": "inherit", "basis": "§4.7：文字卡渲染（5/5 content）" },
    { "dir": "src/risk/", "layer": "automation", "newFile": "inherit", "basis": "§4.7：RiskController 单写（19/19 automation）" },
    { "dir": "src/schema/", "layer": "automation", "newFile": "adjudicate", "basis": "【待定稿裁决】§4.7 基线 sha 之后由 change cloud-schema-migration-executor 新增，src/schema/ 是 §4.7 里不存在的新目录（12 文件）。整目录判 automation，三条理由：① MUST NOT 判 kernel——§4.7 的 kernel 花名册是封闭的 4 文件名单，新增成员 MUST 走「析出 + 回写 §4.7」通道；且 schema-capability.ts（:148 的 pg_indexes 查询）与 pg-catalog.ts（:31 的 pg_class 查询）含 SQL 字面量，AC-BOUND-03 的「无 SQL」准入条件当场不过，与 boundaries/kernel-non-members.json 里 curated-content-store.ts / client-user-store.ts / content-schedule-store.ts 三条被拒条目同一条判据。② MUST NOT 判 composition——AC-BOUND-02 限定 composition 只能是 compositionWhitelist 里的组合根（src/server.ts / src/index.ts），且让 34 个存储反向导入组合根是依赖倒置。③ 落 automation 是最小失真选择——本 change design.md 已写明拆仓后 kernel 由 aidcp-automation 以版本化包发布、api 与 content 固定版本消费，故「尚未获得 kernel 准入的共享基础设施」暂随 automation。newFile 取 adjudicate 而非 inherit：本条是尚未回写 §4.7 的临时归属，§4.7 并没有判定「本目录任意新文件属于哪一层」，后续新增文件（尤其是从 schema-capability.ts 析出的纯判定段，它按消除路径 (a) 恰恰应判 kernel）MUST 重新逐个裁，MUST NOT 由目录默认值接住。故本目录现有 12 个文件亦逐个写进 fileOverrides，不进 adjudicated-files.json 名册（该名册 MUST NOT 增长）。请求回写 §4.7 见 docpatch R4" },
    { "dir": "src/soul/", "layer": "api", "newFile": "inherit", "basis": "§4.7：人设文档模型（7/7 api）" },
    { "dir": "src/storage/", "layer": "content", "newFile": "inherit", "basis": "§4.7：对象存储访问（4/4 content）" },
    { "dir": "src/time/", "layer": "kernel", "newFile": "adjudicate", "basis": "§4.7 kernel 花名册：无业务语义。kernel 花名册按 §4.7 是封闭名单，新增成员 MUST 走「析出 + 回写 §4.7」通道，故本目录新增文件一律待裁决" },
    { "dir": "src/transport/", "layer": "automation", "newFile": "inherit", "basis": "§4.7 基线 sha 之后由 change block2-outbox-transport 新增的目录：拆进程用的跨进程传输原语，整目录归 automation、无逐文件切分、新文件 inherit。MUST 回写 §4.7。【2026-07-29 校订，change split-cloud-automation-production-runtime】原文写的是「异步事件 outbox 传输原语（有 SQL、不进 kernel）」——那只是本目录的**第一批**成员（首个消费者为风控事件、与既有 risk_counter_outbox 同域）。此后目录已长出第二种形态：「服务端注册 + 类型化客户端 + 路径常量」三件套（3a/4a 那两批，以及本 change 的 operator-command-http.ts / content-authority-http.ts），它们**零 SQL**。归属判定不受影响（整目录归 automation 的理由是「跨进程运行时原语、有副作用故不进 kernel」，与是否含 SQL 无关），但生成物里那句 note 会照抄本 basis，不校订就会让后来人以为新文件必须有 SQL 才配进这个目录" }
  ],
  "fileOverrides": [
    { "path": "src/account-display-name.ts", "layer": "api", "basis": "§4.7 src/ 根文件：api 3 / 626 行" },
    { "path": "src/account-state.ts", "layer": "api", "basis": "§4.7 src/ 根文件：api 3 / 626 行" },
    { "path": "src/account-store.ts", "layer": "api", "basis": "§4.7 src/ 根文件：api 3 / 626 行" },
    { "path": "src/deployment-target.ts", "layer": "kernel", "basis": "§4.7 kernel 花名册 4 文件之一（10 行）" },
    { "path": "src/kernel/transport-gate-exemptions.ts", "layer": "kernel", "basis": "change split-cloud-automation-production-runtime 批 D：副本陈旧时传输层出口闸的放行判定。拆进程后两个进程都要问同一个问题（接口进程出口闸 / 自动化进程边-云出口闸），两边各写一份的现形方式不是报错，是某一侧悄悄多扣住一类信封（扣住租约归还 = 槽位永不释放）。纯常量 + 纯函数，刻意不用模块级 new Set（准入正则认写法，类型标注救不了）。api 侧原文件等值再导出，调用点一行未改" },
    { "path": "src/kernel/config-mirror-gate.ts", "layer": "kernel", "basis": "定稿 §4.7 2026-08-01 裁决（change split-cloud-automation-production-runtime）：配置副本停手闸的纯判定段。拆进程后出现第二个读者——自动化进程要判同一件事而够不着 api 属主的实现；两边各写一份的现形方式不是报错，是该停手时没停。零 IO / 零定时器 / 零 SQL / 零模块级可变状态，KERNEL_ADMISSION_CHECKS 实跑通过。api 侧那两份保留属主并改为调它，7 个 api 消费方一行未改" },
    { "path": "src/kernel/soul-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change lift-shared-contracts-to-kernel 抬入：原 src/soul/types.ts（纯人设文档类型/接口/枚举，零 import、零 SQL、零模块级可变单例），被 api/content/automation 三层共 16 处跨边界 type-only 导入。满足 §4.7 kernel 准入（无 SQL / 无 HTTP / 无 LLM / 无进程内活状态 / 不反向依赖业务层）。kernel 花名册是封闭 4 文件名单，本条为新增成员的请求回写，见 docpatch RK1" },
    { "path": "src/kernel/interaction-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change lift-shared-contracts-to-kernel 抬入：原 src/interactions/types.ts（纯互动域类型/接口/枚举，零 import），被 automation/api 两层共 9 处跨边界 type-only 导入。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK1" },
    { "path": "src/kernel/interaction-automation-ports.ts", "layer": "kernel", "basis": "change deploy-derived-services-to-dev 抬入：原 src/interactions/interaction-automation-ports.ts（api）里的 ReplyWorkflowWritePort / InteractionSendPort 两个纯类型端口 + 提交点名单常量。拆进程后这两个端口必须被三方同时看见（api 消费者标注构造参数 / automation 属主按结构兼容提供实例 / aidcp-transport 的 registrar+client 搬同一组方法），而传输包只许引 kernel，否则只能在传输层再声明一份结构相同的——两份声明各自编译通过、各自测试通过，签名漂开时无人会说话。零运行时副作用 / 零 SQL / 只 import kernel 内 interaction-types，KERNEL_ADMISSION_CHECKS 实跑通过。原属主文件保留为残壳并具名再导出（ReplyPreviewBuilderPort 只被 api 内部用，刻意留原处）。请求回写 §4.7" },
    { "path": "src/kernel/persona-format.ts", "layer": "kernel", "basis": "【待定稿裁决】change lift-shared-contracts-to-kernel 抬入：原 src/agents/persona-format.ts（纯人设兴趣格式化函数 tieredInterests，零 import、零副作用），被 6 个 automation 判定/撰写角色跨边界导入。满足 §4.7 kernel 准入。注：原 §4.7『src/agents/ api 2 文件 152 行』的行数配对（与 persona-auto-fill.ts 合计）随本次抬走失效，请求回写 §4.7 见 docpatch RK1" },
    { "path": "src/kernel/title-clamp.ts", "layer": "kernel", "basis": "【待定稿裁决】change lift-shared-contracts-to-kernel 抬入：原 src/publish-agent/title-clamp.ts（纯字形安全标题截断工具，模块级 const 仅 Intl.Segmenter 无 Map/Set/Pool/定时器、零 SQL、零 import），被 2 处跨边界导入。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK1" },
    { "path": "src/kernel/schema-capability-contract.ts", "layer": "kernel", "basis": "【待定稿裁决】change extract-schema-capability-contract 从 src/schema/schema-capability.ts 析出的纯契约段（SchemaCapabilityStatus/SchemaShape/SchemaCapabilityVerdict/SchemaCapabilitySpec 类型 + classifySchemaCapability 纯函数 + SchemaCapabilityError 类 + isSchemaCapabilityError 守卫 + schemaSelfCreateEnabled 读 env），零 import、无 SQL 字面量、无 HTTP/LLM、无进程内活状态（selfCreateWarned 一次性告警单例留在 automation）。满足 §4.7 kernel 准入。连库探测执行段 probeSchemaShape/ensureCapabilitySchema 留 src/schema/schema-capability.ts（automation）。请求回写 §4.7 见 docpatch RK1" },
    { "path": "src/kernel/visual-reference-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change bulk-lift-all-shared-types 整文件抬入：原 src/publish-agent/visual-reference-types.ts（纯视觉参考分析类型/枚举 + 3 个 as const 枚举常量，零 import、零 SQL、零活状态）。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK2" },
    { "path": "src/kernel/yaml.ts", "layer": "kernel", "basis": "【待定稿裁决】change bulk-lift-all-shared-types 整文件抬入：原 src/soul/yaml.ts（域无关极简 YAML 解析器，纯函数，零 import、零 SQL、零活状态）。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK2" },
    { "path": "src/kernel/schema-name.ts", "layer": "kernel", "basis": "【待定稿裁决】change bulk-lift-all-shared-types 整文件抬入：原 src/schema/schema-name.ts（运行时 schema 名解析，纯字符串函数只读 env，零 import、无 SQL 字面量、零活状态）——正是 src/schema/ 消除路径 (a) 里应判 kernel 的纯段。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK2" },
    { "path": "src/kernel/persona-binding.ts", "layer": "kernel", "basis": "【待定稿裁决】change bulk-lift-all-shared-types 从 src/config/persona-store.ts 析出：PersonaBinding 三态纯类型，零 import、零 SQL、零活状态，被 automation/api 多边 type-only 共导。存储实现留 persona-store.ts。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK2" },
    { "path": "src/kernel/feed-action.ts", "layer": "kernel", "basis": "【待定稿裁决】change bulk-lift-all-shared-types 从 src/cache/interaction-feed-store.ts 析出：FeedAction 纯类型联合，零 import、零 SQL、零活状态。存储实现留 interaction-feed-store.ts。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK2" },
    { "path": "src/kernel/pipeline-log-contract.ts", "layer": "kernel", "basis": "【待定稿裁决】change bulk-lift-all-shared-types 从 src/publish-agent/publish-pipeline-log-store.ts 析出：PipelineLogEntry/PipelineLogSink 纯接口契约，零 import、零 SQL、零活状态。含 pg Pool + INSERT 的写入实现留 publish-pipeline-log-store.ts（api）。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK2" },
    { "path": "src/kernel/panel-frame-limits.ts", "layer": "kernel", "basis": "【待定稿裁决】change panel-event-tee-hardening 新增：面板事件单帧大小闸（PANEL_FRAME_MAX_BYTES 常量 + panelPayloadTruncated 截断标记构造 + panelPayloadByteLength 字节口径），零 import、零 SQL、零 HTTP/LLM、零进程内活状态。必须落 kernel 而非任一业务层：该上限有两个执行点——推送端 src/panel/panel-ws.ts（api）与旁路端 src/transport/eventbus-outbox-bridge.ts（automation），各写一份必然漂移成「一端截断、另一端照写几十 KB 进共库生产 PG」；放任一层都会制造一条 api↔automation 跨层边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK2" },
    { "path": "src/kernel/content-schedule-mode.ts", "layer": "kernel", "basis": "【待定稿裁决】change bulk-lift-all-shared-types 从 src/config/content-schedule-store.ts 析出：CONTENT_SCHEDULE_ACTION_MODES 常量 + ContentScheduleActionMode/ContentScheduleApprovalMode 纯类型，零 import、零 SQL、零活状态。排期存储（含建表读写 SQL）留 content-schedule-store.ts。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK2" },
    { "path": "src/kernel/facebook-operation-policy-resolution.ts", "layer": "kernel", "basis": "change split-cloud-automation-production-runtime 批 E-2 步骤 2（同步骤 1 的裁定：析出纯判定段进 kernel）：从 src/config/facebook-operation-policy-store.ts（api）析出「基线取用」判定 + 其纯数据契约（FacebookBaseOperationMode / FacebookPrimaryBrowseSurface / FacebookCadenceSource 三个取值表、rule|consumption|reels 参数组、FacebookOperationPolicyBaseProjection|Resolution、环境解析器签名、三条具名 blocker 串、resolveFacebookOperationBase / cloneBaseline）。存储残壳留 api 持库持缓存写审计并等值再导出，既有消费方零改动。这条判定决定整个 Facebook 浏览模式：拆进程后接口进程按内存缓存问、自动化进程按同步读快照问，各写一份的现形方式不是报错而是某一侧永远答不出基线（下游即账号永远不开始浏览）。零 import、零 SQL / HTTP / LLM / 模块级 Set|Map / 活状态。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-E2-2" },
    { "path": "src/kernel/content-schedule-resolution.ts", "layer": "kernel", "basis": "change split-cloud-automation-production-runtime 批 E-2 步骤 1（用户 2026-08-01 拍板走「析出纯判定段进 kernel」，与配置副本停手闸那次同一条路）：从 src/config/content-schedule-store.ts（api）析出「给定快照 + 账号 → 生效排期」纯段（EffectiveContentSchedule 形状 + resolveEffectiveContentSchedule / resolveEffectiveActiveWeekMask / resolveEffectiveContentActiveMask）。存储残壳留 api 并等值再导出，既有消费方零改动。拆仓后 api 进程按内存镜像问、automation 进程按同步读快照问同一个问题，各写一份的现形方式不是报错而是两侧对同一账号算出不同生效时段。仅 import kernel（content-schedule-mode / week-active-mask），零 SQL / HTTP / LLM / 模块级 Set|Map / 活状态。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-E2-1" },
    { "path": "src/kernel/llm-contract.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-llm-lang-interaction-contracts 从 src/llm/qwen.ts 析出：LlmThinkingModeOpt + LlmCallOpts（Per-call 覆盖选项，纯接口，无供应商语义标识符）。供 automation 侧多角色 type-only 共导，消 4 条 automation->content 边。thinkingMode 字段内联为 'off'|'on'|'default' 字面量（结构与 role-catalog ThinkingMode|'default' 逐字一致），以保 kernel 不反向依赖 api 属主的 role-catalog。LlmClient/ChatLlmClient 因门禁 §4.7 把这两个标识符本身列为「LLM 或供应商 HTTP 调用」特征而**不入 kernel**、留 qwen.ts（那 3 条 LlmClient/ChatLlmClient 边留 residual）。QwenClient 类/错误类/buildXxx 等 behavior 留 qwen.ts。零 SQL/HTTP/fetch/活状态。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch" },
    { "path": "src/kernel/writing-language.ts", "layer": "kernel", "basis": "【待定稿裁决·改判·部分析出】change decouple-llm-lang-interaction-contracts 从 src/soul/writing-language.ts 析出无 Set 依赖的纯函数/纯类型：WritingLanguageCheck / writingLanguageLabel / writingLanguageInstruction / checkWritingLanguage（+私有 HAN_RE/LATIN_RE/VIETNAMESE_MARK_RE 正则字面量与 countMatches），唯一 import 为 kernel/soul-types 的 WritingLanguage 类型，无 SQL/fetch/class/模块级可变单例。被 content(publish-agent prompts/content-creator/publish-executor、facebook-comment-composer-prompt)与 automation(comment-composer/comment-de-ai-flavor)两域共用。原整文件在 kernel-non-members.rejected 以「业务判定」名义拒入；本刀独立核对：checkWritingLanguage 是对显式入参 (text, language) 的无状态文本启发式、非账号级推断/连库，改判其纯函数段可入 kernel。**但 isWritingLanguage + 模块级 WRITING_LANGUAGE_SET（new Set）+ WRITING_LANGUAGE_VALUES 留 src/soul/writing-language.ts（api）**：门禁 §4.7 把模块级 const=new Set 列为「进程内活状态」禁止入 kernel（故不是整文件 git mv，是部分析出；comm/handler 的 isWritingLanguage 边留 residual）。满足 §4.7 kernel 准入。请求控制仓回写 §4.7 kernel 段花名册并核 src/soul/ 行数配对 见 docpatch" },
    { "path": "src/kernel/like-affinity.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-llm-lang-interaction-contracts 整文件 git mv：原 src/soul/like-affinity.ts（唯一 import 为 kernel/soul-types 的 LikeAffinity/Soul 类型，导出全为纯常量+纯函数 DEFAULT_LIKE_AFFINITY/LIKE_AFFINITY_VALUES/resolveLikeAffinity/likeAffinityLabel/noteLikeAffinityGuidance/commentLikeProbability/generatedLikePrinciple，无 SQL/fetch/class/进程内活状态）。被 automation(comment-like-appraiser/interaction-appraiser-role/persona-generator)与 api(account-persona-service)共用。satisfies §4.7 kernel 准入。请求回写 §4.7 见 docpatch" },
    { "path": "src/kernel/interaction-reply-contract.ts", "layer": "kernel", "basis": "【待定稿裁决·部分析出】change decouple-llm-lang-interaction-contracts 析出无模块级 Set 依赖的纯物：从 reply-config.ts（api，§4.6.1）析出 validateFinalReplyText（只读 profile 字段+内联正则，无 Set）；从 reply-config-resolver.ts（api）析出纯读取端口 ReplyConfigReader 与两个无状态读取函数 readPublishedConfig/readJobConfig（只对注入 reader 端口取数、不连库）。**reply-config.ts 其余校验/渲染函数依赖模块级 VARIABLE_SET/HARD_RISK_SET/INTENTS/… 等 const=new Set，被门禁 §4.7 判「进程内活状态」故不入 kernel、留原文件**（reply-auto-send / reply-workflow 引这些的边留 residual）。两 api 原文件保持属主并等值再导出；连库的 ReplyConfigResolver 类留 reply-config-resolver.ts（api）。零 SQL/HTTP/LLM/fetch/活状态。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch" },
    { "path": "src/index.ts", "layer": "composition", "basis": "§4.7 src/ 根文件：组合根，无继承者" },
    { "path": "src/server.ts", "layer": "composition", "basis": "§4.7 src/ 根文件：组合根，无继承者" },
    { "path": "src/config-mirror-freshness.ts", "layer": "api", "basis": "【待定稿裁决】§4.7 基线 sha 之后由 change config-mirror-cross-process-invalidation 新增，§4.7『src/ 根文件』行（api 3 / kernel 1 / composition 2）未覆盖它。按配置域默认落 api（§4.6.8：src/config/ 除四类限频配置外归 api，本文件是同一域的镜像新鲜度查询口，仅因『不让风控层第一次 import 配置层』而放在 src/ 根）。MUST NOT 判 kernel：§4.7 的 kernel 花名册是封闭的 4 文件名单，且本文件 :59 有模块级可变单例 installedSource，AC-BOUND-03 的 kernel 准入判据当场不过。请求回写 §4.7 见 docpatch R1" },
    { "path": "src/db/environment-row-lock.ts", "layer": "api", "basis": "【待定稿裁决】§4.7 基线 sha 之后由 change publish-approval-signal-to-database 新增，src/db/ 是 §4.7 里不存在的新目录。按『锁随被保护数据走』落 api：本文件唯一动作是锁 client_environments 的行，该表 §5.1 具名归 aidcp-api 单写、§4.7 的 src/client-auth/ 亦整目录归 api。此处用 fileOverride 而非目录规则，故 src/db/ 后续新增文件仍会逐个待裁决。请求回写 §4.7 见 docpatch R1" },

    { "path": "src/agents/curated-note-evaluator.ts", "layer": "automation", "basis": "§7.2.1 改判（用户 2026-07-29，change split-cloud-automation-production-runtime）：判据三原依据说的是输出（产物落精选库、不回灌动作链），那部分仍成立；决定归属的是输入——本角色订阅 automation 进程内事件总线的 note.detail.arrived 与图片快照事件，拆仓后这些事件不跨进程，且系统里没有跨进程事件总线（在 content 侧另起第二个角色调度器的方案已被否）。另一条端口修不了的形态：四个角色经 CONTENT_ROLE_FACTORIES 以一张工厂函数表注入角色调度器，是构造输入而非被调用的服务，HTTP 端口包不了工厂函数（与 §10.7 发帖调度器同形）。写 content 属主表按 §5.1 经窄端口，MUST NOT 直连 content 属主库；判据五照常适用。可选的图内文字转写依赖不随迁（转写器留 content，见 §7.2.1），其窄接口与两个纯函数抬 kernel 供两侧共用" },
    { "path": "src/agents/curated-comment-evaluator.ts", "layer": "automation", "basis": "§7.2.1 改判（用户 2026-07-29）：同 curated-note-evaluator——订阅的 comment_like.confirmed 是 automation 进程内事件，且它同属 CONTENT_ROLE_FACTORIES 那张工厂函数表。写精选库经窄端口" },
    { "path": "src/agents/concept-extractor-role.ts", "layer": "automation", "basis": "§7.2.1 改判（用户 2026-07-29）：同上。旁路写概念池、fire-and-forget 这条原依据说的是输出；输入仍是 automation 进程内事件总线的 note.detail.arrived。写概念池经窄端口" },
    { "path": "src/agents/valuable-comment-archivist.ts", "layer": "automation", "basis": "§7.2.1 改判（用户 2026-07-29）：同上。另有一条独立佐证——它归档的 valuableCommentStore（src/cache/valuable-comment-store.ts）属主本就是 automation，即今天存在一条『content 角色写 automation 属主表』的反方向边，任何 blocker 台账都没记它；改判后该反向边一并消失" },
    { "path": "src/agents/persona-generator.ts", "layer": "content", "basis": "§7.2 判据三首批迁移名单" },
    { "path": "src/agents/persona-soul-codec.ts", "layer": "content", "basis": "change split-cloud-api-composition-root-4a: content-local pure validation/serialization for the exact PersonaGenerator output subset; prevents the independent content root from importing the API-owned general Soul loader" },
    { "path": "src/agents/persona-auto-fill.ts", "layer": "api", "basis": "§17 第 7 项待回写：§4.7 记 src/agents/ 有 api 2 文件 152 行但未点名，本文件（136 行）是其一，人设按 §5.1 归 aidcp-api。原配对文件 persona-format.ts 已由 change lift-shared-contracts-to-kernel 抬入 kernel（src/kernel/persona-format.ts），§4.7 的『api 2 文件 152 行』行数配对需一并回写，见 docpatch RK1。待控制仓回写 §4.7 后本行 basis 改为直接引用 §4.7" },

    { "path": "src/cache/curated-content-store.ts", "layer": "content", "basis": "§4.7 src/cache/ 逐文件表" },
    { "path": "src/cache/concept-store.ts", "layer": "content", "basis": "§4.7 src/cache/ 逐文件表" },
    { "path": "src/cache/notification-contact-store.ts", "layer": "api", "basis": "§4.7 src/cache/ 逐文件表" },
    { "path": "src/cache/bot-chat-store.ts", "layer": "api", "basis": "§4.7 src/cache/ 逐文件表" },
    { "path": "src/kernel/pg-config.ts", "layer": "kernel", "basis": "§4.7 kernel 花名册成员（27 行）。change decouple-store-schema-pgconfig-cluster 从 src/cache/pg-config.ts git mv 到 src/kernel/（物理位置归位 kernel 目录，字节等价，唯一 import 是 import type pg，无 SQL / 无业务依赖 / 无进程内活状态）。消费方改为直接从 kernel import DEFAULT_PG_CONFIG，消除经 pg-anchor-cache（automation）再导出的跨边界边。请求回写 §4.7 见 docpatch" },

    { "path": "src/cli/test-feishu.ts", "layer": "api", "basis": "§4.7 src/cli/ 逐文件表：调用面为飞书（§4.6.2）" },
    { "path": "src/cli/trigger-like.ts", "layer": "automation", "basis": "§4.7 src/cli/ 逐文件表：导入边云协议并下发互动命令" },

    { "path": "src/config/quota-config-store.ts", "layer": "automation", "basis": "§4.6.8：四类限频配置随消费方走" },
    { "path": "src/config/pacing-config-store.ts", "layer": "automation", "basis": "§4.6.8：四类限频配置随消费方走" },
    { "path": "src/config/pacing-config-facade.ts", "layer": "automation", "basis": "§4.6.8：门面，无独立表" },
    { "path": "src/config/session-config-store.ts", "layer": "automation", "basis": "§4.6.8：四类限频配置随消费方走" },
    { "path": "src/config/resume-config-store.ts", "layer": "automation", "basis": "§4.6.8：四类限频配置随消费方走" },
    { "path": "src/config/facebook-rule-mode-store.ts", "layer": "api", "basis": "change facebook-rule-mode-cadence：Facebook 规则开关、账号平台校验、审计回读与配置镜像失效均属于 api 配置权威；运行计数和批次已拆到 automation store，本文件只读写 api 属主表" },
    { "path": "src/config/facebook-operation-policy-store.ts", "layer": "api", "basis": "change add-configurable-facebook-consumption-mode：环境基础模式、规则/消费节奏、慢启动锚点与策略审计是 api 配置权威；本文件只写 api 属主表并在同一事务推进配置镜像" },
    { "path": "src/config/facebook-group-comment-policy-store.ts", "layer": "api", "basis": "change add-configurable-facebook-consumption-mode：目标环境的入群后首次评论等待与审计是 api 配置权威；运行时只消费其 target-scoped 投影" },

    { "path": "src/kernel/config-mirror-bump-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change block3-l3-config-mirror-bump-decouple 抬入：跨域配置镜像失效通道的契约段（ConfigMirrorKey 闭集合纯类型 + MirrorQueryable/MirrorVersionBumper 接口 + writeWithMirrorBump「同一笔提交」包装 + ConfigMirrorBumpSink 消费端口）。原分散在 src/config-mirror-freshness.ts（ConfigMirrorKey）与 src/config/mirror-version-store.ts（api）；automation 属主的四个限频配置 store 与 api 属主的十余个配置 store 都要引用它们，留在任一属主域都必然造出跨域 import（原状即 4 条 automation->api 豁免）。只 import type pg 与同层 pg-owner-connection-resolver，无 SQL 字面量（BEGIN/COMMIT/ROLLBACK 是事务控制、不是 DML/DDL）、无 HTTP 路由、无 LLM、无进程内活状态、不反向依赖业务层。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch" },
    { "path": "src/config/api-sync-read-checkpoint-store.ts", "layer": "api", "basis": "change split-cloud-api-composition-root-4b task 2.6：api consumer 自己的 target-scoped sync-read cursor/readiness/health PG adapter，只写 api_sync_read_consumer_checkpoint；owner payload 与共享业务 version 不落表。公共校验/恢复状态机在 kernel，automation 使用独立 owner-local adapter，避免拆库后跨池写。" },
    { "path": "src/config/mirror-bump-outbox.ts", "layer": "automation", "basis": "【待定稿裁决】change block3-l3-config-mirror-bump-decouple 新增：automation 侧的失效信号事务型入队器（OutboxMirrorVersionBumper，在四个限频配置 store 自己的写事务里写本域 event_outbox 行）+ 生产方进程内中继（ConfigMirrorBumpRelay）。写的是 automation 属主的 event_outbox、跑在 automation 池上，与同目录四个限频配置 store（§4.6.8 的 5 个例外文件）同域，故判 automation。src/config/ 在 §4.7 是逐文件切分目录，故逐条裁定、不进 adjudicated-files 名册" },
    { "path": "src/config/mirror-bump-sink.ts", "layer": "api", "basis": "【待定稿裁决】change block3-l3-config-mirror-bump-decouple 新增：api 侧失效信号落地端（config_mirror_bump_inbox 去重 + config_mirror_version 推进，同一笔 api 事务）。两张表都属 api、跑在 api 池上，与同目录 mirror-version-store.ts / mirror-registry.ts / mirror-refresher.ts 同层，故判 api" },
    { "path": "src/config/mirror-registry.ts", "layer": "api", "basis": "§4.6.8 例外只有四类限频配置 store + 门面，本文件不在其列，故随 src/config/ 默认落 api。它是 15 处进程内配置镜像的描述符表（写入权归属只用于文档与断言，不影响运行时装配）。由 change config-mirror-cross-process-invalidation 新增" },
    { "path": "src/config/mirror-version-store.ts", "layer": "api", "basis": "§4.6.8 同上：配置层的版本表读写口，承载 config_mirror_version / config_mirror_stale_refusal 两张配置面表。由 change config-mirror-cross-process-invalidation 新增" },
    { "path": "src/config/mirror-refresher.ts", "layer": "api", "basis": "§4.6.8 同上：配置镜像的轮询刷新器，装配在组合根、只驱动配置层 store 的 refreshFromAuthority。由 change config-mirror-cross-process-invalidation 新增" },
    { "path": "src/config/mirror-stop-work.ts", "layer": "api", "basis": "【待定稿裁决】§4.6.8 的例外名单是封闭的 5 个文件，本文件不在其列，故按目录默认落 api。但它的语义偏自动化：它是『闸门镜像陈旧则不放行新的真实平台动作』的统一判据，消费方是 comment-scheduler / role-dispatcher / delegated-task worker 三个 automation 文件（因此产生 3 条 automation->api 豁免）。若按 §4.6.8『随消费方走』的同一判据改判 automation，这 3 条消失、另生 3 条（publish-scheduler 的 api->automation 1 条 + 本文件对 config-mirror-freshness / mirror-registry 的 automation->api 2 条），净额为 0，故本次不擅自改判。请求 §4.6.8 裁决见 docpatch R1。由 change config-mirror-cross-process-invalidation 新增" },

    { "path": "src/interactions/interaction-auth-gate.ts", "layer": "api", "basis": "【待定稿裁决】change block3-l3-auth-write-owner-collapse 新写：kernel 端口 InteractionAuthGate 的 api 属主实现（PgInteractionAuthGate），持组合根注入的 api 池、**自己开事务**，只读 / 只锁 api 属主表 client_environments / client_env_scope（经同层 db/environment-row-lock）与 client_env_revocation_holds / accounts（§5.1「客户、环境归属和账号绑定」+「账号主数据」全归 api）。src/interactions/ 目录规则 layer=automation 但 newFile=adjudicate，故逐文件裁定：本文件与同目录 reply-config-store / interaction-api-writes 一样，目录位置不是属主判据、它碰的表全是 api 属主，归 api。它是 offboard-cleanup-grant-ops.ts（automation 属主实现、api 侧经端口转调）的**反方向镜像**。请求回写 §4.7 见 docpatch RK-block3" },
    { "path": "src/interactions/interaction-api-writes.ts", "layer": "api", "basis": "§5.1：reply 配置面（reply_templates/reply_rules/account_reply_profiles/interaction_reply_config*）与配置面审计 interaction_audit_events 归 aidcp-api 单写。本文件是 change offboard-saga 里这些 api 属主表在离场/过期清理路径上的 api 侧单写者，automation 经窄接口注入调用。" },
    { "path": "src/interactions/interaction-customer-api.ts", "layer": "api", "basis": "§4.6.1：配置存储与查询面" },
    { "path": "src/interactions/interaction-automation-ports.ts", "layer": "api", "basis": "§4.6.1：配置/查询面（api）消费 automation 编排行为的窄注入端口（consumer-owned ports）。change decouple-interaction-api-cluster 新增：把 interaction-customer-api 对 send-orchestrator / reply-workflow 的直接 import type 收窄为端口接口，只引 kernel 契约、由组合根按结构兼容注入 automation 实例，automation 类不 import 本端口。" },
    { "path": "src/interactions/interaction-internal-api.ts", "layer": "api", "basis": "§4.6.1：配置存储与查询面" },
    { "path": "src/interactions/offboard-write-adapter.ts", "layer": "automation", "basis": "§5.1：互动域离场表（interaction_offboards/interaction_offboard_audit/interaction_auth_state/interaction_runtime_controls）归 aidcp-automation 单写。本文件是 change offboard-saga 里这些离场表的 automation 侧单写者，aidcp-api 的 client-user-store 经窄接口注入调用。" },
    { "path": "src/interactions/interaction-scope-internal-api.ts", "layer": "api", "basis": "§4.6.1：配置存储与查询面" },
    { "path": "src/interactions/interaction-panel-permissions.ts", "layer": "api", "basis": "§4.6.1：配置存储与查询面" },
    { "path": "src/interactions/reply-config-store.ts", "layer": "api", "basis": "§4.6.1：配置存储与查询面" },
    { "path": "src/interactions/reply-config-scope-store.ts", "layer": "api", "basis": "§4.6.1：配置存储与查询面" },
    { "path": "src/interactions/reply-config.ts", "layer": "api", "basis": "§4.6.1：配置存储与查询面" },
    { "path": "src/interactions/reply-config-resolver.ts", "layer": "api", "basis": "§4.6.1：配置存储与查询面" },
    { "path": "src/interactions/reply-ai.ts", "layer": "content", "basis": "§4.6.1：AI 回复段，唯一引 LlmClient 的文件" },

    { "path": "src/onboarding/first-post-onboarding-store.ts", "layer": "api", "basis": "§4.7：状态表归 api（151 行）" },
    { "path": "src/onboarding/first-post-onboarding-coordinator.ts", "layer": "content", "basis": "§4.7：创作桥接归 content（105 行）" },

    { "path": "src/orchestrator/content-scheduler.ts", "layer": "api", "basis": "§4.7 / §4.6.5 第 12 项：内容排期调度器归 api" },
    { "path": "src/orchestrator/facebook-consumption-mode.ts", "layer": "automation", "basis": "change add-configurable-facebook-consumption-mode：消费模式收据分类、阈值推进与 revision 仲裁直接服务 automation 浏览闭环，不持配置写权" },
    { "path": "src/orchestrator/facebook-consumption-mode-coordinator.ts", "layer": "automation", "basis": "change add-configurable-facebook-consumption-mode：消费模式的 like/join/comment 单飞编排、提交前复核与恢复直接驱动真实平台动作，归 automation" },
    { "path": "src/orchestrator/facebook-consumption-mode-runtime-store.ts", "layer": "automation", "basis": "change add-configurable-facebook-consumption-mode：target-scoped 唯一浏览事实、进度、动作义务、回执去重与终态是 automation 运行权威；本文件只写 automation 属主表" },
    { "path": "src/orchestrator/facebook-consumption-mode-types.ts", "layer": "automation", "basis": "change add-configurable-facebook-consumption-mode：消费运行进度、动作义务与回执形状只描述 automation 域内编排与持久运行事实" },
    { "path": "src/orchestrator/facebook-rule-mode.ts", "layer": "automation", "basis": "change facebook-rule-mode-cadence：有效模式仲裁与规则浏览卡片选择直接服务 automation 浏览闭环，不持配置权威、不写数据库" },
    { "path": "src/orchestrator/facebook-rule-mode-runtime-store.ts", "layer": "automation", "basis": "change facebook-rule-mode-cadence：target-scoped 浏览事实、进度、批次和动作终态是 automation 运行权威，本文件只读写 automation 属主表" },

    { "path": "src/publish-agent/publish-log-store.ts", "layer": "api", "basis": "§4.6.3：台账与审批段 7 文件之一" },
    { "path": "src/publish-agent/publish-ui-update-producer.ts", "layer": "api", "basis": "change split-cloud-api-composition-root-4a: API owner reads the publish_log preview/state locally and emits only the typed one-way UI update command; no SQL or automation implementation is imported" },
    { "path": "src/publish-agent/publish-pipeline-log-store.ts", "layer": "api", "basis": "§4.6.3：台账与审批段 7 文件之一" },
    { "path": "src/publish-agent/publish-scheduler.ts", "layer": "automation", "basis": "【待定稿裁决 · 用户 2026-07-26 拍板】原判 api（§4.6.3「台账与审批段 7 文件之一」）。改判 automation 的**唯一理由是一条结构约束，不是就近原则**：委托任务的执行器（server.ts:5894 的守卫）**同时**需要评论调度器与本调度器，前者是 automation 属主；两者一旦分处两进程，那条 if 恒不成立，表现为 /delegate 收得下、存得住、永远不执行（启动期只有一条 warn，之后每条任务静默堆积）。它读的三类数据本来就跨三个域（点赞记录 automation / 精选库与概念池 content / 发布台账 api），所以留在哪一侧都要跨——改判只是换跨的方向，却省掉了委托执行链的跨进程重构。**改判零代价的机械依据**：本文件的 import 全部指向 kernel（15-27 行逐条核过），依赖一律靠构造注入的接口，故换层不产生任何新的跨层边；入边只有组装根（composition→任意层恒允许）。同段其余 6 个文件（publish-log-store / publish-pipeline-log-store / client-publish-approval 等）**维持 api 不动**——它们是真的台账与审批。请求回写 §4.6.3 见 docpatch R-batch3-scheduler" },
    { "path": "src/publish-agent/client-publish-approval.ts", "layer": "api", "basis": "§4.6.3：台账与审批段 7 文件之一" },
    { "path": "src/publish-agent/draft-image-remove.ts", "layer": "api", "basis": "§4.6.3：台账与审批段 7 文件之一" },
    { "path": "src/publish-agent/schedule-policy.ts", "layer": "api", "basis": "§4.6.3：台账与审批段 7 文件之一" },
    { "path": "src/publish-agent/roles/approval-gatekeeper.ts", "layer": "content", "basis": "§4.6.3：生成候选段（2026-07-25 修正——按本节自述机械判据「引 ../llm/ 即生成段」，且不写 publish_log/授权记录）" },
    { "path": "src/publish-agent/publish-approval-store.ts", "layer": "api", "basis": "§4.6.3 的机械 import 指纹判据第三条（『直接写 publish_log 或授权记录即为台账与审批段』）：本文件是 publish_approval_decision 的唯一持久写者，而 §4.6.3 正文另有直接点名『授权事实由新建的 publish_approval_decision（aidcp-api 单写）承担』。由 change publish-approval-signal-to-database 新增" },
    { "path": "src/publish-agent/publish-approval-outlet.ts", "layer": "api", "basis": "§4.6.3 同上：五个审批入口收敛到这里写同一条授权记录，属台账与审批段。由 change publish-approval-signal-to-database 新增" },
    { "path": "src/publish-agent/publish-approval-api.ts", "layer": "api", "basis": "§4.6.3 同上：授权的窄内部接口，按未来 HTTP 形状写死、阶段 4 提取 aidcp-api 时把进程内适配器换成 HTTP 客户端。由 change publish-approval-signal-to-database 新增" },
    { "path": "src/publish-agent/publish-approval-outbox-relay.ts", "layer": "api", "basis": "【待定稿裁决】change split-cloud-api-composition-root-3b 新写：API 单写的 publish_approval_outbox 可靠中继，只读未确认 PublishApproved 并在 automation trigger 明确短应答后确认消费；断链或结果未知保留原命令。直接依赖 API owner PublishApprovalStore，故随授权台账归 API，不与 automation receiver 共文件。请求回写 §4.6.3。" },
    { "path": "src/publish-agent/pending-dispatch-watchdog.ts", "layer": "api", "basis": "【待定稿裁决】§4.6.3 的三条 import 指纹判据对本文件都不命中：它不引 ../comm/（非下发段）、不引 ../llm/ 或图片供应商（非生成段）、也不写 publish_log 或授权记录（只读授权台账行）。按『离哪一段最近』落 api：数据面是 api 属主的 publish_approval_decision，全部依赖经注入、无自有 SQL。反向理由是它探测的失联对象在 automation 侧、且告警落 alerts（§4.6.4 判归 automation）。当前落 api 时新增跨边界边为 0，落 automation 则新增 1 条。请求 §4.6.3 逐文件回写见 docpatch R1。由 change publish-approval-signal-to-database 新增" },
    { "path": "src/publish-agent/curated-gate.ts", "layer": "automation", "basis": "§7.2.1 改判（用户 2026-07-29，change split-cloud-automation-production-runtime）：随 curated_note_evaluator / curated_comment_evaluator 两个角色改判 automation——两者值引用它的共鸣预筛与配置解析，而它在全库只有这两个角色 + 组装根三个消费者（组装根 src/server.ts:7225 那处也在 segCAutomation 段内）。迁移代价为零：本文件零 import。顺带坐实一条谁都没记的边——resolveCuratedGateConfig 今天在 automation 段被调用而文件属 content，automation 仓里根本没有它，任何 blocker 台账都没记；改判后就地消失。目录规则 src/publish-agent/ 默认 content，故必须逐文件点名" },
    { "path": "src/publish-agent/publish-dispatcher.ts", "layer": "automation", "basis": "§4.6.3：下发执行段 6 文件之一" },
    { "path": "src/publish-agent/publish-dispatch-trigger.ts", "layer": "automation", "basis": "【待定稿裁决】change split-cloud-api-composition-root-3b 新写：automation owner 的短应答批准触发接收器；校验 target/revision 后只唤醒本进程 PublishDispatcher，并区分 decision_recorded 去重与 human_reconfirm 清熔断语义。进程内 Set 是受理去重状态，不能进 kernel；不再依赖 API owner store。请求回写 §4.6.3。" },
    { "path": "src/publish-agent/command-sequencer.ts", "layer": "automation", "basis": "§4.6.3：下发执行段 6 文件之一" },
    { "path": "src/publish-agent/platform-profile.ts", "layer": "automation", "basis": "§4.6.3：下发执行段 6 文件之一" },
    { "path": "src/publish-agent/fill-budget.ts", "layer": "automation", "basis": "§4.6.3：下发执行段 6 文件之一" },
    { "path": "src/publish-agent/scheduled-publish-reconciler.ts", "layer": "automation", "basis": "§4.6.3：下发执行段 6 文件之一" },
    { "path": "src/publish-agent/roles/publish-executor.ts", "layer": "content", "basis": "【改判 2026-07-26 · change cloud-coupling-phase5】原判「§4.6.3 下发执行段 6 文件之一」，实测对 §4.6.3 自己写的三条机械判据**全不命中下发段**：引 ../comm/ 只命中 feishu-card-contract（审批卡结构化合同、非边缘协议与租约，且已随本 change 抬入 kernel）；引 ../llm/ 与图片供应商零命中；publish_log / INSERT / UPDATE / query( 零命中（落库经注入的 PublishLogStore 端口）。运行事实同向：组合根 server.ts 把它 registerRole 进 publishOrchestrator（content 的生成编排器）而非 PublishDispatcher；它是 roles/ 目录 31 个角色里唯一被判 automation 的一个。误判根源与 approval-gatekeeper（2026-07-25 修正）同型：change decouple-publish-generation-from-dispatch 把生成与下发切开后，真正的下发挪进 PublishDispatcher，本文件退化成「生成候审段的出口」（其文件头自述：不再驱动边缘指令序列、不再解析边缘节点），而归属表记的仍是切分前的形态。反证同样是硬的：坚持留 automation 就得把 BasePublishRole + PipelineContext 抬进 kernel，两者都是带管线行为的类、撞「kernel 里不放行为类」。改判后本文件全部 import 皆为 content 或 kernel，消除 import 豁免 2 条。请求回写 §4.6.3 见 docpatch RK-P5-3" },

    { "path": "src/schema/schema-capability.ts", "layer": "automation", "basis": "【待定稿裁决】层随 src/schema/ 目录规则判 automation（三条理由见该规则的 basis）。本文件是 src/schema/ 里跨层被共用的主入口：34 个存储的 init() 导入它的 ensureCapabilitySchema，api / content / automation 三边都有，本轮新增的跨边界豁免绝大多数由它**单点**产生（20 条：api→automation 16 + content→automation 4；本目录另有 schema-name.ts 贡献 1 条，其余 10 个文件 0 条）。由它产生的这些豁免共享**同一条消除路径**——§4.7 一次裁决即可全部消除，消除方式二选一：(a) 把纯判定段（classifySchemaCapability / 错误形状 / 自建旋钮）析出为独立**无 SQL** 的新文件后判 kernel，探测执行段（pg_catalog / pg_indexes 查询）留 automation；(b) 确认 automation 归属 + 按拆仓设计以版本化包供 api / content 固定版本消费。**MUST NOT 把这些豁免当成 20 处各自独立、需要各自消除的架构违规**：它们是同一个共享基础设施的同一次归属待裁，条目数多只反映消费方多，逐条各挂一个消除 change 会把一次裁决摊成 20 次无意义的返工。请求回写 §4.7 见 docpatch R4" },
    { "path": "src/schema/db-scope-scan.ts", "layer": "automation", "basis": "【待定稿裁决】层随 src/schema/ 目录规则判 automation。只被 src/schema/ 内部、scripts/ 与测试导入，不产生跨层边。请求回写 §4.7 见 docpatch R4" },
    { "path": "src/schema/ddl-objects.ts", "layer": "automation", "basis": "【待定稿裁决】同上：只被 src/schema/ 内部、scripts/ 与测试导入，不产生跨层边。请求回写 §4.7 见 docpatch R4" },
    { "path": "src/schema/ddl-scan.ts", "layer": "automation", "basis": "【待定稿裁决】同上：只被 src/schema/ 内部、scripts/ 与测试导入，不产生跨层边。请求回写 §4.7 见 docpatch R4" },
    { "path": "src/schema/migration-files.ts", "layer": "automation", "basis": "【待定稿裁决】同上：只被 src/schema/ 内部、scripts/ 与测试导入，不产生跨层边。请求回写 §4.7 见 docpatch R4" },
    { "path": "src/schema/migration-owners.ts", "layer": "automation", "basis": "【待定稿裁决】change schema-ledger-per-owner 新增：「一条迁移属于哪个属主库」的唯一判据（迁移头 -- aidcp:objects= 声明的对象所落的表 → boundaries/table-ownership.json 的属主）。层随 src/schema/ 目录规则判 automation，MUST NOT 判 kernel——它读文件系统（migrations/ 与 boundaries/table-ownership.json）、有 I/O 副作用，AC-BOUND-03 的 kernel 准入当场不过。只被 src/schema/schema-gate.ts、scripts/ 与测试导入，不产生跨层边。请求回写 §4.7 见 docpatch R4" },
    { "path": "src/schema/migration-order.ts", "layer": "automation", "basis": "【待定稿裁决】同上：只被 src/schema/ 内部、scripts/ 与测试导入，不产生跨层边。请求回写 §4.7 见 docpatch R4" },
    { "path": "src/schema/migration-plan.ts", "layer": "automation", "basis": "【待定稿裁决】同上：只被 src/schema/ 内部、scripts/ 与测试导入，不产生跨层边。请求回写 §4.7 见 docpatch R4" },
    { "path": "src/schema/pg-catalog.ts", "layer": "automation", "basis": "【待定稿裁决】同上：只被 src/schema/ 内部与测试导入，不产生跨层边。注：本文件含 pg_catalog 的 SELECT 字面量，是 src/schema/ MUST NOT 判 kernel 的两个直接证据之一。请求回写 §4.7 见 docpatch R4" },
    { "path": "src/schema/schema-contract.ts", "layer": "automation", "basis": "【待定稿裁决】同上：只被 src/schema/ 内部、scripts/ 与测试导入，不产生跨层边。请求回写 §4.7 见 docpatch R4" },
    { "path": "src/schema/schema-gate.ts", "layer": "automation", "basis": "【待定稿裁决】层随 src/schema/ 目录规则判 automation。只被组合根 src/server.ts 与测试导入（组合根 MAY 导入任何层，不产生豁免）；它自身引 src/cache/pg-anchor-cache.ts 的 DEFAULT_PG_CONFIG，同属 automation，亦无跨层边。请求回写 §4.7 见 docpatch R4" },
    { "path": "src/schema/schema-inspect.ts", "layer": "automation", "basis": "【待定稿裁决】同上：只被 src/schema/ 内部、scripts/ 与测试导入，不产生跨层边。请求回写 §4.7 见 docpatch R4" },
    { "path": "src/kernel/platform-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-platform-type-closure 从 src/platform/registry.ts 析出纯类型契约段（PlatformId / Surface / NoteScopedAction / OrchestrationCapability / DelegatedAction(Support) / ScheduledAutomationAction(Support/Mode) / AvailableScheduledAutomationAction / NoteSupport / CommentPlatformProfile / PlatformRegistryEntry），零 SQL / 零 HTTP / 零 LLM / 零模块级活状态。PLATFORM_REGISTRY 数据 + *_COMMENT_PROFILE 常量 + 读表函数（normalizePlatformId/platformRegistryEntry/commentProfileForPlatform/…）按 §9「平台能力由 aidcp-automation 单写」留 registry.ts（automation）。SCHEDULED_AUTOMATION_ACTIONS 运行时数组留 registry.ts 并 satisfies 本联合。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK3" },
    { "path": "src/kernel/text-card-style.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-platform-type-closure 从 src/render/palettes.ts + src/render/text-card.ts 析出纯设计令牌类型（PaletteKey / LayoutVariant / Decoration / TextCardBackgroundTreatment / TextCardBackgroundPattern / TextCardBulletPresentation / TextCardSourceStyle），零 import、零 SQL、零活状态。色板 hex 数据 PALETTES + selectTheme/fnv1a 渲染逻辑留 render/（content），并以 satisfies 让 PALETTES/LAYOUT_VARIANTS/DECORATIONS 逐字对齐本联合。为发布管线闭包（publish-pipeline-types）能进 kernel 而析出。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK3" },
    { "path": "src/kernel/curated-content-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-platform-type-closure 从 src/cache/curated-content-store.ts 析出纯数据模型类型（CuratedReferenceImageFormGuess / TextCardTranscription(*) / CuratedReferenceImage(Input) / CuratedObservation / CuratedSelectItem / CuratedPanelRow / CuratedFacets / CuratedContentType(Filter) 等），仅 import kernel（visual-reference-types / time）。CURATED_CONTENT_SCHEMA_SQL + CuratedContentStore 类 + normalize* 函数 + CuratedContentStoreOptions 留 curated-content-store.ts（content）。满足 §4.7 kernel 准入（无 SQL）。请求回写 §4.7 见 docpatch RK3" },
    { "path": "src/kernel/delegated-task-types.ts", "layer": "kernel", "basis": "【待定稿裁决·部分析出】change decouple-platform-type-closure 从 src/delegated-task/types.ts 析出纯类型（DelegatedTask / DelegatedTaskIntent / DelegatedTaskAttempt / DelegatedTaskStatus / DelegatedVerificationKind / DelegatedExecutionTarget / Json* / TaskConstraints 等），仅 import kernel（platform-types / deployment-target）。**状态机模块级 Set（TERMINAL_STATUSES / TRANSITIONS）+ 转移/校验函数 + 运行时数组 DELEGATED_TASK_STATUSES 留 delegated-task/types.ts（automation）**：门禁 §4.7 禁 kernel 持模块级 new Set（故非整文件搬）。DELEGATED_TASK_STATUSES satisfies kernel 联合。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK3" },
    { "path": "src/kernel/publish-pipeline-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-platform-type-closure 从 src/publish-agent/types.ts 整体 git mv：发布管线纯类型契约（TriggerInput / PublishRecord / PublishMetadata / PipelineFields / ImagePlan / CoverCardPlan 等 + IMAGE_CATEGORIES as-const 枚举 + METADATA_DEFAULT_VALUES 保守默认 + hasUserRejectionEvidence 纯守卫），仅 import kernel（soul-types / curated-content-types / content-schedule-mode / platform-types / text-card-style / visual-reference-types / pipeline-log-contract / deployment-target）。零 SQL / HTTP / LLM / 模块级 Set/Map。原路径 publish-agent/types.ts 保留为 re-export 桩，content 层内部消费方无感。消 8 条跨边界入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK3" },
    { "path": "src/kernel/panel-automation-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change block3-l3-panel-read-port 新写：面板（api 属主 panel-store）对 automation 域的只读投影端口契约（PanelAutomationReader 接口 + PanelActionTotal / PanelAccountActionTotal / PanelLikeViewTotal / PanelRiskStateProjection / PanelAlertProjection / PanelInteractionProjection 纯行类型）。零 import、零 SQL / HTTP / LLM / 模块级 Set-Map。与 risk-read-types（账号维风控状态投影端口）同类：读方（api）只依赖本 kernel 接口、绝不直连 automation 的库；属主实现在 src/risk/panel-automation-read.ts（automation）跑在 automation 池上。Block③ 物理拆库 L3 面板读侧解耦。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-block3" },
    { "path": "src/kernel/valuable-comment-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-behavior-class-ports 从 src/cache/valuable-comment-store.ts 析出纯物：ValuableCommentInput（纯数据模型）+ topicKeysFromTitle（无 LLM 主题键派生纯字符串函数，函数体内局部 Set 非模块级单例）。零 import、零 SQL、无 pg、无进程内活状态。VALUABLE_COMMENT_SCHEMA_SQL + ValuableCommentStore 类 + pg 读写留 valuable-comment-store.ts（automation）。供 content 侧精选评估角色跨边界共导，消 3 条 content->automation 边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch" },
    { "path": "src/kernel/review-card-delivery-port.ts", "layer": "kernel", "basis": "【待定稿裁决】change cloud-batch2-content-main 新写：候审卡是否发飞书的跨属主判定口（ReviewCardDeliveryPort 接口 + ReviewCardDeliveryDecision 纯结果类型）。零 import、零 SQL / HTTP / LLM / 模块级 Set-Map。判定要读两张 api 属主表（账号分组稿件投递策略 + 客户审批归属可证性），整段留 api 侧实现，content 侧只依赖本接口 ⇒ content 因此不再需要 approvalPolicyStore 与 clientUserStore 两个 apiPool 绑定的存储。语义含 fail-open 契约（判不出来一律保留飞书卡），写在接口文档里、两端共守。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-batch2" },
    { "path": "src/kernel/publish-log-writer-port.ts", "layer": "kernel", "basis": "【待定稿裁决】change cloud-batch2-content-main 新写：发布台账的窄写入口（PublishLogWriter，四个方法 insert/updateStatus/recordMetadata/markImagesAttached）。零 SQL / HTTP / LLM / 模块级 Set-Map，仅 import kernel（publish-pipeline-types 的 PublishRecord / PublishStatus）。publish_log 是 api 属主表而写它的是内容域发布出口 ⇒ 拆进程后这条边走内部 HTTP，两端 MUST 共用同一份定义。形状照抄属主存储真实签名、不另造更宽松入参类型；init() 有意不在口上（属主探测自己 schema，内容域无立场）。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-batch2"},
    { "path": "src/kernel/publish-card-exit-port.ts", "layer": "kernel", "basis": "【待定稿裁决】change cloud-batch2-content-main 新写：发布候审的卡片出口端口（PublishCardExitPort，六方法 sendApprovalCard/sendCommandResult/uploadImageFromUrl/getDefaultChat/resolveCardChatId/writeApprovalSignal）。零 SQL / HTTP / LLM / 模块级 Set-Map，仅 import kernel（feishu-card-contract / default-chat-provider）。实现全在 api 域（飞书客户端与卡片构造 src/feishu/、机器人会话表、授权台账），内容域只交结构化数据 ⇒ **aidcp-content 因此不需要飞书 SDK**，与批次 1 依赖集重算把飞书判给 api 的结论一致（此前内容段直调飞书，两者只能有一个成立）。失败语义：六方法一律原样抛（发卡失败由发布出口角色自己接住并如实记账；授权写失败吞掉即「以为已授权其实没写」；落点解析失败要让调用方诚实回没有目标）。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-batch2"},
    { "path": "src/kernel/image-model-selection-port.ts", "layer": "kernel", "basis": "【待定稿裁决】change cloud-batch2-content-main 新写：图片出口的模型选择端口（ImageModelSelection 纯结果类型 + ImageModelSelectionSource 异步取源口 + ImageModelSelectionReader 同步读口）。零 import、零 SQL / HTTP / LLM / 模块级 Set-Map。形状与本批其余端口不同的原因写在文件头：调用点是**同步**的且在热闭包里（每次配图取一次厂商 / 模型），改成 await 一次 HTTP 会改掉每个调用点签名并给热路径加一跳网络 ⇒ 跨进程形态是「异步取源 + 同步读本地镜像」两件事。单体下镜像即配置存储进程内缓存，两口同源逐字等价。镜像的诚实要求（从未取到过回保守默认、刷新失败保留上一份好值）写在接口文档里。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-batch2"},
    { "path": "src/kernel/role-model-selection-port.ts", "layer": "kernel", "basis": "【待定稿裁决】change cloud-batch2-content-main 新写：角色模型解析端口（RoleModelSelection / RoleModelSelectionSnapshot 纯结果类型 + Source 异步取源口 + Reader 同步读口）。零 import、零 SQL / HTTP / LLM / 模块级 Set-Map。解析要读三张 api 属主配置表（role_config / category_config / model_config）且分类归属来自 api 的角色目录，而调用点是**同步**的、在每次模型调用路径上 ⇒ 属主侧把**已登记角色逐个预解析**成快照再送，调用方查表；送表会要求调用方复刻四层回落，正是两侧各写一份必然漂移的形态。thinkingMode 内联 'off'|'on' 字面量而不 import 角色目录同名类型，照 llm-contract.ts 既有判例（kernel MUST NOT 反向依赖 api 属主的角色目录）。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-batch2"},
    { "path": "src/kernel/provider-secret-port.ts", "layer": "kernel", "basis": "【待定稿裁决】change cloud-batch2-content-main 新写：厂商密钥窄读口（ProviderSecretReader，单方法 getSecretForRuntime）。零 import、零 SQL / HTTP / LLM / 模块级 Set-Map。provider_credentials 是 api 属主表而用密钥的是内容域的文本 / 图片 / 对象存储出口；本口只在启动期被调几次、不在热路径 ⇒ 普通异步跨进程读，**不需要镜像**。契约明写「读不到 ≠ 没配」：缺凭据回 null、读失败必须抛（吞成 null 会让调用方静默少构造一个出口，表现为链路悄悄不工作且零报错）。明文只用于构造出口客户端，MUST NOT 日志化 / 回前端 / 落盘。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-batch2"},
    { "path": "src/kernel/model-config-defaults.ts", "layer": "kernel", "basis": "【待定稿裁决】change cloud-batch2-content-main 从 src/config/model-config-store.ts（api 属主）析出**纯值**：ModelConfigValue 形状 + MODEL_CONFIG_DEFAULTS 缺省默认。零 import、零 SQL / HTTP / LLM / 模块级 Set-Map。析出理由是拆进程后出现了第二个读者——内容进程的图片模型选择镜像与角色模型解析镜像都要「从未取到过时的保守默认」，而它们在另一个仓、够不着那个存储；两边各写一份字面量会得到「各自编译通过、各自测试通过，只在属主侧改了默认模型名后才现形，且现形方式是配图悄悄用了另一个（可能已下架的）模型」的静默漂移。建表 SQL / 内存缓存 / 镜像版本推送仍留属主存储（残壳原位具名再导出，既有导入面逐字不变）。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-batch2" },
    { "path": "src/kernel/facebook-group-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-behavior-class-ports 从 src/comment-agent/facebook-group-store.ts 析出纯数据模型类型闭包（FacebookGroupTargetRow / *ListRow / *ListOptions / *ListResult / *Facets / MembershipRow / *ScopeWriteRow / ReplaceFacebookGroupTargetScopesResult / JoinAuditOutcome / JoinRecentScheduledResult / ScopedTargetCount / ImportResult / AccountProgress + JoinGating/MembershipStatus 枚举）+ FacebookGroupScopeError（纯 Error 子类，与 schema-capability-contract 的 SchemaCapabilityError 同一手法）。零 import、零 SQL、无 pg。FacebookGroupStore 类 + SCHEMA_SQL + pg 读写 + DbRow 内部映射留 facebook-group-store.ts（automation）。供 api 侧面板/配置视图跨边界共导，消 3 条 api->automation 边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch" },
    { "path": "src/kernel/schedule-policy.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-longtail-sweep 从 src/publish-agent/schedule-policy.ts 整体 git mv：纯发布排期策略（XHS_SCHEDULE_MIN/MAX_AHEAD_MS 常量 + ScheduleValidationError 联合 + validatePublishSchedule 纯校验函数），仅 import kernel（platform-types / publish-pipeline-types）。零 SQL / HTTP / LLM / 模块级 Set-Map。原路径 publish-agent/schedule-policy.ts 保留为 re-export 桩，同层消费方无感。消 2 条跨边界入边（automation command-sequencer / content publish-mode-decider）。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-longtail" },
    { "path": "src/kernel/reference-image-guidance.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-longtail-sweep 从 src/publish-agent/reference-image-guidance.ts 整体 git mv：纯参考图引导（REFERENCE_IMAGE_MAX_COUNT 常量 + referenceImages* / referenceImageUrl / buildReferenceImageGuidance 纯字符串/过滤函数），仅 import kernel（publish-pipeline-types）。零 SQL / HTTP / LLM / 模块级 Set-Map。原路径 publish-agent/reference-image-guidance.ts 保留为 re-export 桩，同层消费方无感。消 1 条 api 侧 publish-scheduler 入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-longtail" },
    { "path": "src/kernel/note-detail.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-longtail-sweep 从 src/agents/content-curator-role.ts 析出 NoteData 纯数据模型接口（noteId/title/content/author/likeCount/collectCount/authorFollowed/comments，全为原语与数组）。零 import、零 SQL、零 HTTP、零 LLM、无进程内活状态。ContentCuratorRole 类 + 事件订阅留 content-curator-role.ts（automation）并等值再导出。供 content 侧精选评估角色跨边界共导，消 2 条 content->automation 边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-longtail" },
    { "path": "src/kernel/account-identity.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-longtail-sweep 从 src/account-store.ts 析出 RETIRED_ACCOUNT_ID 退役保留账号哨兵字符串常量（'default'）。零 import、零 SQL、无进程内活状态。account-store.ts 从 kernel 导入并等值再导出，内部判定复用。供 publish-agent 发布媒体存储跨边界比对，消 1 条入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-longtail" },
    { "path": "src/kernel/account-projection-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change automation-accounts-projection 新增：账号身份跨属主投影的端口契约（AccountIdentityProjectionRow 三列纯类型 + AccountRosterSourcePort 只读无参端口）。零 import、零 SQL / HTTP / LLM、无进程内活状态、不反向依赖业务层。accounts 属 api 单写，automation 域为把守卫读拉回同库而在本域冷备投影，投影来源经本端口取，避免 automation 直连 api 的库。api 侧 src/account-store.ts 实现、automation 侧 src/transport/account-projection-store.ts 消费。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-account-projection" },
    { "path": "src/kernel/facebook-comment-config-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-longtail-sweep 从 src/config/facebook-comment-config-store.ts 析出纯数据模型类型（FacebookContainer / FacebookCommentMode / EffectiveFacebookCommentConfig），零 import、零 SQL、无进程内活状态。SCHEMA_SQL + FacebookCommentConfigStore 类 + coerce/校验函数留 facebook-comment-config-store.ts（automation）并从 kernel 导入等值再导出。供 automation 侧评论调度器跨边界共导，消 1 条入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-longtail" },
    { "path": "src/kernel/facebook-rule-mode-types.ts", "layer": "kernel", "basis": "change facebook-rule-mode-cadence：跨 api 配置面与 automation 运行面共享的固定规则定义、纯 DTO 和结果 union；零 import、零 SQL、零活状态，避免任一属主反向导入另一属主实现" },
    { "path": "src/kernel/image-provider-registry.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-longtail-sweep 从 src/publish-agent/image-providers.ts 析出图片厂商注册表纯数据段 + 无状态归一（ImageProviderId / ImageProviderMeta / DEFAULT_IMAGE_PROVIDER / IMAGE_PROVIDERS 字面数据 / isKnownImageProvider / normImageProvider）。零 import、零 SQL / HTTP / LLM、零模块级 Set-Map（IMAGE_PROVIDERS 为字面 Record，归一用 Object.hasOwnProperty）。路由客户端类 RoutingImageProvider（依赖 image-provider.js 的 ImageProvider 客户端接口）留 image-providers.ts（content）并从 kernel 导入等值再导出。供 api 侧面板版本视图 / 配置外观跨边界共导，消 2 条入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-longtail" },
    { "path": "src/kernel/comment-approval-notice.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-longtail-sweep 从 src/agents/comment-approval-gate.ts 析出 CommentApprovalNoticeInput 纯输入数据模型（requestId/noteId/text/title/authorName/accountId/accountName + approvalSource union，全为原语）。零 import、零 SQL、无进程内活状态。CommentApprovalGate 角色留 comment-approval-gate.ts（automation）并从 kernel 导入等值再导出。供飞书卡片层跨边界共导，消 1 条入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-longtail" },
    { "path": "src/kernel/mandatory-comment-notice.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-longtail-sweep 从 src/orchestrator/role-dispatcher.ts 析出 MandatoryCommentOutcome 结局 union + MandatoryCommentOutcomeNoticeInput 纯输入数据模型（全为原语与 union）。零 import、零 SQL、无进程内活状态。RoleDispatcher 实现留 role-dispatcher.ts（automation）并从 kernel 导入等值再导出（未触碰 RoleName / 角色注册）。供飞书卡片层跨边界共导，消 1 条入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-longtail" },
    { "path": "src/kernel/publish-approval-contract.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-longtail-sweep 从 src/publish-agent/publish-approval-store.ts 析出发布审批纯枚举 + 哨兵错误（APPROVAL_VOID_REASONS / ApprovalVoidReason / isApprovalVoidReason / APPROVAL_BLOCKED_REASONS / ApprovalBlockedReason 字面 tuple 与派生 union；ApprovalUnreadableError 纯 Error 子类，与 schema-capability-contract 的 SchemaCapabilityError 同一手法）。零 import、零 SQL、无 pg、无模块级 Set-Map。ApprovalDecisionStore 类 + pg 读写留 publish-approval-store.ts（automation）并从 kernel 导入等值再导出。供发布下发编排跨边界共导，消 1 条入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-longtail" },
    { "path": "src/kernel/publish-generation-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change block2-content-process-split（2e batch1）新增：内容生成触发窄面端口契约（PublishGenerationPort），仅 import kernel 的 publish-pipeline-types（TriggerInput / PublishResult）。零 SQL / HTTP / LLM / 无模块级 Set-Map。默认 local 时组合根注入 PublishOrchestrator 本体、http 时注入 PublishGenerationHttpClient，两者结构兼容本接口。为拆进程后 automation 经端口远程触发 content 生成而立。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-process-split" },
    { "path": "src/kernel/publish-status-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change block2-content-process-split（2e batch1）新增：发布队列状态读侧窄面端口契约（PublishStatusReader，getStatus 异步读），零 import。零 SQL / HTTP / LLM / 无进程内活状态。默认 local 时组合根把 PublishOrchestrator.getStatus 适配为异步端口注入、http 时注入 PublishStatusHttpClient。为后台/客户端经数据网关读发布状态而立。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-process-split" },
    { "path": "src/kernel/risk-read-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change block2-api-split 新增：风控只读窄面端口契约（RiskReadPort，getState / effectiveQuotas / slowStartView 异步读），零 import。零 SQL / HTTP / LLM / 无进程内活状态。风控最终状态单写仍留 automation 的 apply-callback；api/panel 经本端口取只读投影（默认 local 直连本地 RiskController、http 经 RiskReadHttpClient）。为拆进程后 api 侧读风控态而立。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-process-split" },

    { "path": "src/kernel/facebook-group-join-prompt.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-behavior-class-ports 从 src/agents/facebook-group-join-judge.ts（automation）析出纯 prompt 构建段：FacebookGroupJoinObservation 观测数据模型 + FacebookGroupJoinPhase 枚举 + buildFacebookGroupJoinJudgePrompt 纯函数（入参→prompt 字符串）。零 import、零 SQL、零 LLM、无进程内活状态。判定类 FacebookGroupJoinJudge（确定性短路 / LLM 调用 / 审计回写）留 facebook-group-join-judge.ts（automation）并从 kernel 等值再导出。供 api 侧静态 prompt 预览跨边界共导，消 1 条 api->automation 入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-prompt-final" },
    { "path": "src/kernel/facebook-comment-composer-prompt.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-behavior-class-ports 从 src/comment-agent/facebook-comment-composer-prompt.ts 整体 git mv：整文件为纯 Facebook 评论撰写 prompt 构建（FacebookCommentComposerPromptInput + buildFacebookCommentComposerPrompt），唯一 import 为 kernel 的 soul-types 类型与 writing-language 指令。零 SQL / HTTP / LLM / 模块级 Set-Map。原路径 comment-agent/facebook-comment-composer-prompt.ts 保留为 re-export 桩，automation 层内部消费方无感。消 1 条 api->automation 入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-prompt-final" },
    { "path": "src/kernel/interaction-reply-prompt.ts", "layer": "kernel", "basis": "【待定稿裁决·部分析出】change decouple-behavior-class-ports 从 src/interactions/reply-ai.ts（content）析出纯 prompt 构建段：buildInteractionReplyPrompt + requiresKnowledgeAnswer 纯判定 + 输出 schema 常量 + InteractionReplyInput/InteractionReplyRole 类型，仅 import kernel 互动域类型。原文件里 KNOWLEDGE_QUESTION_INTENTS 为模块级 new Set（门禁 §4.7 判「进程内活状态」禁入 kernel），此处改写为只读数组 + .includes，语义逐字等价。LLM 调用类 ReplyAiService、解析/纠正 prompt（依赖 LlmClient / setTimeout）留 reply-ai.ts（content）并从 kernel 等值再导出。供 api 侧静态 prompt 预览跨边界共导，消 1 条 api->content 入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-prompt-final" },
    { "path": "src/kernel/cover-form-sense-prompt.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-behavior-class-ports 从 src/publish-agent/cover-form-sensor.ts（content）析出 buildCoverFormSensePrompt 纯函数（零入参、返回固定视觉判定指令）。零 import、零 SQL、零 LLM、无进程内活状态。感知服务 createCoverFormSensor / 多模态调用 / 缓存回写留 cover-form-sensor.ts（content）并从 kernel 等值再导出。供 api 侧静态 prompt 预览跨边界共导，消 1 条 api->content 入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-prompt-final" },
    { "path": "src/kernel/cover-card-copy-prompt.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-behavior-class-ports 从 src/publish-agent/prompts.ts（content）析出 buildCoverCardCopyPrompt 纯构建函数与其依赖的 buildContentVisualExcerpt（有界正文摘录纯字符串函数）+ CONTENT_VISUAL_EXCERPT_MAX 常量。零 import、零 SQL、零 LLM、无模块级 Set-Map。prompts.ts 其余多角色构建（依赖 content 兄弟模块 types / content-visual-brief）留原文件（content）并从 kernel 等值再导出这两个纯物。供 api 侧静态 prompt 预览跨边界共导，消 1 条 api->content 入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-prompt-final" },
    { "path": "src/kernel/content-visual-brief.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-behavior-class-ports 从 src/publish-agent/content-visual-brief.ts 整体 git mv：整文件为正文视觉 brief 的纯归一/兜底/格式化函数与纯数据（normalizeContentVisualBrief / ensureContentVisualCategory / createFallbackContentVisualBrief / formatContentVisualCategoryBrief / categorySafetyInstruction / inferContentVisualKind / CONTENT_VISUAL_CATEGORY_LABELS 等），唯一 import 为 kernel 的 visual-reference-types 类型。零 SQL / HTTP / LLM / 模块级 Set-Map（nonEmpty 内的 new Set 为函数体局部量、非模块级单例）。原路径 publish-agent/content-visual-brief.ts 保留为 re-export 桩，content 层内部消费方无感。为 visual-fidelity-audit-prompt 能进 kernel 而抬入（其构建依赖本文件两个格式化函数）。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-prompt-final" },
    { "path": "src/kernel/visual-fidelity-audit-prompt.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-behavior-class-ports 从 src/publish-agent/visual-fidelity-auditor.ts（content）析出 VisualAuditInput 审核输入模型 + buildVisualFidelityAuditPrompt 纯函数（入参→视觉模型文本指令），仅 import kernel（visual-reference-types 类型 + content-visual-brief 的 categorySafetyInstruction/formatContentVisualCategoryBrief）。零 LLM 调用 / 零连库 / 无进程内活状态。审核服务 createVisualFidelityAuditor / 多模态组装 / 解析留 visual-fidelity-auditor.ts（content）并从 kernel 等值再导出。供 api 侧静态 prompt 预览跨边界共导，消 1 条 api->content 入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-prompt-final" },
    { "path": "src/kernel/visual-reference-prompts.ts", "layer": "kernel", "basis": "【待定稿裁决】change decouple-behavior-class-ports 从 src/publish-agent/visual-reference-analyzer.ts（content）析出 buildVisualReferenceSetPrompt + buildVisualReferenceSpecialistPrompt 两个纯函数（入参→视觉模型文本指令），仅 import kernel 的 visual-reference-types 枚举/类型。零 LLM 调用 / 零连库 / 无进程内活状态。分析服务 createVisualReferenceAnalyzer / 多模态调用 / 解析缓存留 visual-reference-analyzer.ts（content）并从 kernel 等值再导出。供 api 侧静态 prompt 预览跨边界共导，消 1 条 api->content 入边。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-prompt-final" },
    { "path": "src/kernel/pg-owner-connection-resolver.ts", "layer": "kernel", "basis": "【待定稿裁决】change Block③ 物理拆库 L2 前置：按 owner 解析 PG 连接配置的纯函数 resolveOwnerPgConfig。只 import type pg + kernel 的 DEFAULT_PG_CONFIG（pg-config.ts），零 SQL 字面量 / 零 HTTP / 零 LLM / 无进程内活状态 / 不反向依赖业务层。物理拆库后三属主服务各连各自库、组合根按 owner 取池，本模块收成唯一「owner→PoolConfig」解析口；owner URL 未配时逐字回落共享单库配置。满足 §4.7 kernel 准入（封闭花名册新增成员，请求回写 §4.7）。" },
    { "path": "src/kernel/client-env-automation-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change block3-l3-client-user-read-port 新写：客户环境生命周期（api 属主 client-auth/client-user-store）对 automation 域的只读投影端口契约（ClientEnvAutomationReader 接口 + OffboardProjection / EnvRiskStateProjection 纯行类型 + OffboardProjectionState/Reason 字面量联合）。零 import、零 SQL / HTTP / LLM / 模块级 Set-Map。与 panel-automation-types（面板专用跨表聚合读端口）同类、形状各按消费方定：读方（api）只依赖本 kernel 接口、绝不直连 automation 的库；属主实现在 src/interactions/client-env-automation-read.ts（automation）跑在 automation 池上。Block③ 物理拆库 L3 客户环境读侧解耦。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-block3" },
    { "path": "src/interactions/client-env-automation-read.ts", "layer": "automation", "basis": "【待定稿裁决】change block3-l3-client-user-read-port 新写：kernel 端口 ClientEnvAutomationReader 的 automation 属主实现（PgClientEnvAutomationRead），跑在组合根注入的 automation 池上，只读 automation 属主表 interaction_offboards / interaction_auth_state / risk_state（SQL 从 client-user-store 逐字迁来）。src/interactions/ 目录规则 layer=automation 但 newFile=adjudicate，故逐文件裁定：本文件是同目录 offboard-write-adapter.ts（离场表单写者）的读侧配对，同域同段（§4.6.1 离场 saga 段），归 automation。请求回写 §4.7 见 docpatch RK-block3" },
    { "path": "src/kernel/interaction-auth-gate-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change block3-l3-auth-write-owner-collapse 新写：互动域环境授权闸的**跨域操作**端口契约（InteractionAuthGate 接口 + 回执 / 输入输出纯类型 + 拒绝档与环境串行档字面量联合）。零 import、零 SQL / HTTP / LLM / 模块级 Set-Map。与 offboard-cleanup-grant-types 同族且方向相反：那一条是 api 调 automation，本条是 automation 调 api。文件头载明「闸在 api、写在 automation ⇒ 两笔 + 条件写回执」的判据，以及回执**能**与**不能**保证什么。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-block3" },
    { "path": "src/kernel/interaction-audit-outbox.ts", "layer": "kernel", "basis": "【待定稿裁决】change block3-l3-auth-write-owner-collapse 新写：配置面审计（interaction_audit_events，§5.1 归 api 单写）的跨属主投递契约 —— outbox 主题名 / 中继消费者名 / 保留期常量 + 审计行纯 DTO + 纯函数解码守卫。零 import、零 SQL / HTTP / LLM / 模块级活状态。被 automation 侧（interaction-store 入队）与 api 侧（interaction-api-writes 落地）共同引用，正是 kernel 段该承载的共享契约。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-block3" },
    { "path": "src/kernel/offboard-cleanup-grant-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change block3-l3-grant-collapse 新写：离场清理授权（cleanup grant）签发/烧票的**跨域操作**端口契约（OffboardCleanupGrantOperations 接口 + OffboardGrantRecord / Issue-Consume 输入输出纯类型 + 状态/原因/失败档字面量联合）。零 import、零 SQL / HTTP / LLM / 模块级 Set-Map。与 client-env-automation-types（只读投影端口）互补：本端口是**写操作**端口，且方法**不接调用方事务句柄、自成一笔事务**——因为这两笔事务碰的表全是 automation 属主、与任何 api 写都不共提交，故可整体收回属主域；属主实现在 src/interactions/offboard-cleanup-grant-ops.ts（automation）跑在 automation 池上。文件头载明搬迁 MUST 逐字保留的五条不变量（判定顺序 / 失败也提交 / 取行加锁与烧票同事务 / now 可注入 / epoch ms）。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-block3" },
    { "path": "src/interactions/offboard-cleanup-grant-ops.ts", "layer": "automation", "basis": "【待定稿裁决】change block3-l3-grant-collapse 新写：kernel 端口 OffboardCleanupGrantOperations 的 automation 属主实现（PgOffboardCleanupGrantOps），持组合根注入的 automation 池、**自己开事务**，只写 automation 属主表 interaction_offboards / interaction_offboard_audit（三条 SQL 从同目录 offboard-write-adapter.ts 逐字迁来并内化）。src/interactions/ 目录规则 layer=automation 但 newFile=adjudicate，故逐文件裁定：本文件与 offboard-write-adapter.ts 同域同段（§4.6.1 离场 saga 段），差别只在「自开事务」vs「接调用方句柄」，归 automation。请求回写 §4.7 见 docpatch RK-block3" },
    { "path": "src/interactions/offboard-admission-reconciler.ts", "layer": "automation", "basis": "【待定稿裁决】change split-cloud-api-composition-root-4a D9 新写：automation 编排本地完整 active offboard 快照、本地物化与 API admission ledger 三项窄端口；不持 pool/事务句柄，不吞 result_unknown，归 automation owner。请求回写 §4.7。" },
    { "path": "src/kernel/offboard-materialization-types.ts", "layer": "kernel", "basis": "【待定稿裁决】change block3-l3-offboard-eventual-consistency 新写：离场**执行台账物化**的跨域操作端口契约（OffboardMaterializationOperations 接口 + MaterializeEnvironmentOffboardInput / Outcome 纯类型 + 原因字面量联合）。唯一 import 是同层 client-env-automation-types 的 OffboardProjection 行类型；零 SQL / HTTP / LLM / 模块级 Set-Map。与同族两端口互补：client-env-automation-types 是只读投影、offboard-cleanup-grant-types 是清理授权的签发/烧票，本端口是台账物化。三者都**不接调用方事务句柄、各自成一笔事务**——这正是拆库后仍成立的判据。属主实现在 src/interactions/offboard-write-adapter.ts（automation）跑在 automation 池上。本端口取代了原 api 侧的 client-auth/offboard-write-port.ts（那个端口的方法接 api 事务句柄，于是 api 的连接去跑 automation 的 SQL，拆库即 42P01 或写进错的库；该文件随本 change 删除）。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-block3" },
    { "path": "src/kernel/week-active-mask.ts", "layer": "kernel", "basis": "【待定稿裁决】P3-1 从 src/risk/session-limits.ts 析出周历掩码的**纯时间判定**段（WEEK_ACTIVE_MASK_LEN / isValidWeekActiveMask / mondayBasedDayIndex / isWeekActiveAt / msUntilNextActive + 私有 HOUR_MS）。零 import、零 SQL/HTTP/LLM/模块级活状态，AC-BOUND-03 四条正则逐条实跑通过。原文件在拒入名册上（理由是「承载会话预算的业务判定」，不是四条正则），故**只析出不整搬**、残壳留 src/risk/ 保持 automation 并具名再导出，composition 与 automation 侧消费方一行不改。请求回写 §4.7 见 docpatch RK-P3-1" },
    { "path": "src/kernel/text-provider-registry.ts", "layer": "kernel", "basis": "【待定稿裁决】P3-2 从 src/llm/providers.ts 析出文本厂商的**身份段**（TextProviderId / DEFAULT_TEXT_PROVIDER / TEXT_PROVIDER_META / isKnownProvider / normProvider / isAllowedCredential）。端点 baseUrlDefault / baseUrlEnv **留 content**（厂商 URL 撞准入禁令③），原文件保留局部端点表并合成等值再导出。照既有判例 src/kernel/image-provider-registry.ts；超出该判例的是 credentialField / envKeys 两字段——api 侧凭据面要的正是它们。请求回写 §4.7 见 docpatch RK-P3-2" },
    { "path": "src/kernel/hot-lead-gate-config.ts", "layer": "kernel", "basis": "【待定稿裁决】P3-3 从 src/hot-lead/heat-velocity.ts 析出过滤闸的**旋钮形状 + 未配置回落值**（HotLeadGateConfig / DEFAULT_HOT_LEAD_GATE_CONFIG）。「什么算热帖」的判定（parsePublishedHoursAgo / heatVelocity / evaluateHotLead）留 automation——整文件搬会与 session-limits 同理被拒。请求回写 §4.7 见 docpatch RK-P3-3" },
    { "path": "src/kernel/account-ownership-port.ts", "layer": "kernel", "basis": "【待定稿裁决】P3-4 从 src/risk/ownership.ts 析出账号归属的窄端口与占位结果类型（AccountOwnershipPort / ClaimExecutionTargetResult）。**放 kernel 不放 api**：automation 的连接握手路径也持有该接口，放 api 会新造 automation→api 反向边、净额 0。**与在册豁免 note 的口径分歧需知**：那条 note 写的消除路径是「随合同回到 api 一侧」，本条选了 kernel，理由如上，已显式对账。原文件按复核修正只留具名再导出，composition 组合根与 automation 侧持有方**一个字节不改**。请求回写 §4.7 见 docpatch RK-P3-4" },
    { "path": "src/kernel/config-stop-work-reasons.ts", "layer": "kernel", "basis": "【待定稿裁决】P3-5 从 src/config/mirror-stop-work.ts 析出两个**具名停手原因码**常量。整文件进不了 kernel（模块级 ReadonlySet = new Set(...)，类型标注不救；且自身 import 两个 api 文件），故只析出常量并把消费方 repoint——只留原位再导出是消不掉边的。请求回写 §4.7 见 docpatch RK-P3-5" },
    { "path": "src/kernel/text-card-transcription.ts", "layer": "kernel", "basis": "【待定稿裁决】P3-6 从 src/cache/curated-content-store.ts 析出文字卡转写结果的**边界归一**（normalizeTextCardTranscription + 其调用闭包内的全部守卫与通用 helper）。存储与 SQL 留 content，属主文件改为 import 回来而非复制（复制会让两份实现漂）。**别并进 curated-content-types.ts**：那是纯类型文件，塞运行时函数会让它产生 runtime 产物。请求回写 §4.7 见 docpatch RK-P3-6" },
    { "path": "src/kernel/ai-flavor-detection.ts", "layer": "kernel", "basis": "【待定稿裁决】P3-7 从 src/publish-agent/prompts.ts + post-processor.ts 析出去 AI 味的**检测与一轮收敛**（BANNED_PHRASES 词表 / exclamationMaxForTone / detectBannedPhrases / aiScoreFromHits / runAiFlavorPass）。评论侧与发帖侧要的是同一套判据，此前评论侧为复用直连 content 实现文件。词表尤其不能各留一份——发帖 prompt 与后处理检测共用同一份是既有不变量。**有意不搬 PostProcessor 类**：本层现有 6 个导出类全是错误类型，且判例 src/kernel/image-provider-registry.ts 文件头明写「路由客户端类留 content」；改为把类里被共用的那段无状态纯编排提成函数 runAiFlavorPass，类留原处当薄壳，两侧共用同一份逻辑、无任何行为复制。重写器由调用方注入，本层不认识模型客户端。请求回写 §4.7 见 docpatch RK-P3-7" },
    { "path": "src/kernel/config-panel-ports.ts", "layer": "kernel", "basis": "【待定稿裁决】P2-1 从 src/panel/types.ts 析出后台四张配置表（quota_config / pacing_floor_config / session_config_global / resume_config_global）的**窄写接口契约与纯载荷类型**（PanelQuotaConfig / PanelPacingConfig / PanelSessionLimits / PanelResumeConfig + 各自 View / PatchInput / SetResult + SessionInteractionBudget）。接口由 api 持有、由 automation 的四个外观实现，两侧都要引：留在 api 会让 automation 侧四个外观各造一条反向边。纯类型 + 接口，四禁全过，唯一 import 是同层 risk-read-types.js（档位/动作枚举不重抄第三份）；pacing 的 operation 按 §10.9 收窄成裸串、词表校验留 automation 外观。请求回写 §4.7 见 docpatch RK-P2-1" },
    { "path": "src/kernel/facebook-group-ops-types.ts", "layer": "kernel", "basis": "【待定稿裁决】split-cloud-api-composition-root-3a 从 Facebook 群面板依赖中收窄出 automation→api 的十方法单向 operation port。只含接口与纯载荷类型，唯一 import 指向同层 facebook-group-types.ts；不含 SQL、HTTP、LLM 或模块级活状态。刻意排除 importTargets / replaceTargetScopes：两者在新团队标签上会反向刷新 api 属主账号花名册，须留到 4a 双向配对。请求回写 §4.7 见控制仓该 change 的 §10 交付记录。" },
    { "path": "src/kernel/group-route-types.ts", "layer": "kernel", "basis": "【待定稿裁决】P2-2 从 src/cache/group-route-store.ts 析出团队→飞书群路由的**纯载荷类型**（GroupRoute / SetGroupRouteResult）。存储本体（建表 SQL / 连接池 / 单写读回）留 automation，原文件 import 回来并等值再导出，automation 桶 cache/index.ts 的 export * 与既有消费方一个字节不改。面板（api）此前经 automation 桶间接拿这两个形状，repoint 到本文件后消 1 条 api->automation 入边。零 import，AC-BOUND-03 四条正则逐条实跑通过。请求回写 §4.7 见 docpatch RK-P2-2" },
    { "path": "src/kernel/llm-usage-types.ts", "layer": "kernel", "basis": "【待定稿裁决】P2-2 从 src/metrics/token-usage-store.ts 析出模型用量**读侧载荷**六型（LlmUsageQuery / LlmUsagePayload + 其传递依赖 LlmUsageRow / LlmUsageBucket / LlmUsageCostEstimate / LlmUsageCostPricingBasis）。少抬任一个即编译不过。**有意不抬** LlmBillingPriceSnapshotInput / LlmBillingPriceTarget：那是写侧计价对账形状、面板不需要。SQL / 连接池 / 聚合查询留 content，原文件 import 回来并等值再导出。消 1 条 api->content 入边（计入 involvingContent）。零 import，四条正则逐条实跑通过。请求回写 §4.7 见 docpatch RK-P2-2" },
    { "path": "src/kernel/billing-price-refresh-types.ts", "layer": "kernel", "basis": "【待定稿裁决】P2-2 从 src/metrics/billing-price-refresh.ts 析出账单价刷新的**回执四型**（BillingPriceRefreshResult + BillingPriceRefreshWritten / BillingPriceRefreshSkip / BillingPriceRefreshSkipReason）。**止步于回执**：同文件的 BillingPriceRefreshTokenUsage / Credentials / Options 是实现侧注入契约，其中 TokenUsage 传递依赖 content 的用量写侧形状，抬进 kernel 会把本层反手拽回业务层。厂商账单读取与价快照落库留 content，原文件 import 回来并等值再导出。消 1 条 api->content 入边（计入 involvingContent）。零 import，四条正则逐条实跑通过。请求回写 §4.7 见 docpatch RK-P2-2" },
    { "path": "src/kernel/alert-resolution-port.ts", "layer": "kernel", "basis": "【待定稿裁决】P2-2 新写：告警「按 id 勾销」的**窄操作端口**（AlertResolutionPort.resolveById）。**有意不搬 AlertStore 整型**——它传递依赖 AlertSeverity（automation 的 alert-notification.ts），整体抬入会造 kernel->automation 反向边、AC-BOUND-03 当场判失败。面板此前写 Pick<AlertStore,'resolveById'> 才拉进整份告警合同；改指本端口后消 1 条 api->automation 入边。同进程期由 PgAlertStore 结构性满足，组合根 src/server.ts 注入点零改动；签名漂移会在组合根那处赋值当场编译不过。与 offboard-cleanup-grant-types / interaction-auth-gate-types 同族（跨域**操作**端口）。零 import，四条正则逐条实跑通过。请求回写 §4.7 见 docpatch RK-P2-2" },
    { "path": "src/kernel/event-fanout-port.ts", "layer": "kernel", "basis": "【待定稿裁决】P2-3 新写：面板 BFF 订阅进程内事件流所需的**最小只读端口**（EventFanoutPort.onAny + PanelEventHandler 回调签名，含跨进程回放的 originTs 可选参）。**不是把总线搬进 kernel**——总线实现持进程内订阅表，已在 kernel-non-members.json 的 rejected 里明确拒入，本条只析出 api 侧真正用到的那一个方法的形状；写能力（emit / emitRaw / on / off / removeAllListeners）一律留 automation。零 import、零 SQL / HTTP / LLM / 模块级活状态，AC-BOUND-03 四条正则实跑零命中。automation 的总线结构上已满足本端口，组合根 src/server.ts 一行不改。消除 import 豁免 2 条（src/panel/types.ts 与 src/panel/panel-ws.ts -> src/event-bus/index.ts，api -> automation）。请求回写 §4.7 见 docpatch RK-P2-3" },
    { "path": "src/kernel/facebook-publish-media-types.ts", "layer": "kernel", "basis": "【待定稿裁决】P2-7 从 src/publish-agent/facebook-publish-media-store.ts 析出 Facebook 发帖素材池的**跨域契约段**：状态枚举常量 FACEBOOK_PUBLISH_MEDIA_STATUSES + 五个纯 DTO（FacebookPublishImageInput / ImageView / ImageSetView / MediaListView / UploadResult / SetPatch）+ 错误原因联合 FacebookPublishMediaErrorReason + 跨进程识别键 FACEBOOK_PUBLISH_MEDIA_ERROR_NAME 与结构化守卫 isFacebookPublishMediaError。**整文件进不了 kernel**：属主文件自建 pg 池（new Pool）、含建表 DDL 字面量、走对象存储，四条准入判据两条当场不过，故只析出无依赖的契约段、存储实现留 content 并等值再导出。**错误类 FacebookPublishMediaError 亦不进 kernel**（Error 子类是行为；判例 llm-contract.ts / interaction-reply-contract.ts 同样把类留属主）：面板层改用结构化守卫按 name+reason 两字段识别，跨进程/跨包都成立——原 err instanceof FacebookPublishMediaError 拆进程后恒 false，会把 object_store_unavailable(503) / status_locked(409) 静默退化成 unavailable(400)，属「MUST NOT 静默假成功」红线。零 import、零 SQL、零 HTTP/LLM、零进程内活状态。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-P2-7" },
    { "path": "src/kernel/risk-contract.ts", "layer": "kernel", "basis": "【待定稿裁决】P2-6 把 src/risk/types.ts（112 行、零 import）整文件提升为风控词表共享契约：三组枚举 RISK_ACTIONS / RISK_STATUSES / RISK_QUOTA_LEVELS 及其联合类型、配额结构 ActionQuota / WindowQuotas、两个注入端口 QuotaProvider / AccountNurtureProvider、RiskState / RiskSignal / CounterEvent 与两个存储端口 RiskStore / InteractionStore，共 18 个导出。**整搬不析出**：AC-BOUND-03 四条正则逐条实跑不命中、零 import；与 session-limits.ts / resume-limits.ts 被拒的理由（承载会话预算的业务判定）不同类，本文件只是词表与端口形状、无任何判定。**RiskSignalKind 一并搬**：路线图原稿称它在 risk-state-machine.ts 属误记，实为本文件；搬走后 risk-state-machine.ts 与 risk-controller.ts 一个字节不改，风控最终状态单写域不受影响。原文件留纯再导出壳（automation），risk/ 内部 17 个消费者与 test/ 零改动；五个跨层消费方 panel/version.ts、panel/panel-server.ts、panel/panel-store.ts、client-auth/client-auth-server.ts、config/quota-config-facade.ts 全部 repoint 到本文件。请求回写 §4.7 见 docpatch RK-P2-6" },
    { "path": "src/config/quota-config-facade.ts", "layer": "automation", "basis": "§4.6.8：门面，无独立表（定稿已判 automation；翻转与 P2-1 同批落——P2-1 把 Panel*Config 四个窄写接口提进 src/kernel/config-panel-ports.ts 之后，本门面对 api 的 type-only 反向边归零）" },
    { "path": "src/config/session-config-facade.ts", "layer": "automation", "basis": "§4.6.8：门面，无独立表（定稿已判 automation；翻转与 P2-1 同批落——P2-1 把 Panel*Config 四个窄写接口提进 src/kernel/config-panel-ports.ts 之后，本门面对 api 的 type-only 反向边归零）" },
    { "path": "src/config/resume-config-facade.ts", "layer": "automation", "basis": "§4.6.8：门面，无独立表（定稿已判 automation；翻转与 P2-1 同批落——P2-1 把 Panel*Config 四个窄写接口提进 src/kernel/config-panel-ports.ts 之后，本门面对 api 的 type-only 反向边归零）" },
    { "path": "src/kernel/persona-ports.ts", "layer": "kernel", "basis": "【待定稿裁决】P4-1 新写：人设通道的三个窄端口（PersonaWritePort / PersonaGeneratorPort / AccountPersonaPort）+ 传输层两个上限常量 + PersonaGenerateInput/Outcome（从 src/agents/persona-generator.ts 整段抬入，原位等值再导出）。端口按**调用方视角**收窄：实现侧成功分支上的 view / summary 字段刻意不进 kernel（那是面板目录视图闭包，整搬会把面板契约拖进共享层），结构可赋值方向上多余字段无害。唯一 import 是 kernel→kernel 的 soul-types.js。四条准入正则实跑零命中。消除 import 豁免 3 条（handler.ts -> panel/types.ts、handler.ts -> agents/persona-generator.ts、handler.ts -> config/account-persona-service.ts）+ 1 条（config/account-persona-service.ts -> agents/persona-generator.ts）。请求回写 §4.7 见 docpatch RK-P4-1" },
    { "path": "src/kernel/default-chat-provider.ts", "layer": "kernel", "basis": "【待定稿裁决】P4-1 新写：默认群目标的单方法只读端口（DefaultChatProvider.getDefaultChat）+ DefaultChatTarget。chatName 保留是因为调用点把它打进「默认群查询完成」日志，砍掉即静默抹掉一条可观测性。零 import，四条准入正则实跑零命中。消除 import 豁免 1 条（src/comm/handler.ts -> src/cache/bot-chat-store.ts，automation -> api），并顺带取代 publish-executor 里那份 3 行手抄同名接口。请求回写 §4.7 见 docpatch RK-P4-1" },
    { "path": "src/kernel/account-pause-port.ts", "layer": "kernel", "basis": "【待定稿裁决】P4-1 新写：账号暂停态三态枚举 AccountPauseState（从 src/account-state.ts 抬入、原位等值再导出）+ 单方法只读端口 AccountPausePort.pauseStateOf。pause / resume / init / refreshFromAuthority / getStatus 全部不进端口。零 import，四条准入正则实跑零命中。消除 import 豁免 1 条（src/comm/handler.ts -> src/account-state.ts，automation -> api）。请求回写 §4.7 见 docpatch RK-P4-1" },
    { "path": "src/kernel/persona-soul-parse.ts", "layer": "kernel", "basis": "【待定稿裁决】change deploy-derived-services-to-dev 从 src/agents/persona-soul-codec.ts（content）析出的**纯解析那一段**：人设闭子集（identity / interests / writing_language / behavior_guidelines）的结构校验，外加同步读 account_persona 流的取值口（解析 + JSON 归一 + 失败回 null 三件事收在一处）。**必须进 kernel 而不是留在任一属主**：同一份人设文本被两个进程各解一遍时，同游标发出两种载荷摘要，消费方按「同游标必同载荷」整条拒收，而两侧各自的行为测试全绿（dev 切流演练实测，游标 902）。序列化那一半留在 content（PersonaGenerator 专用形态），残壳按引用取用本文件。只 import kernel 的 like-affinity / soul-types / sync-read-snapshot（仅类型）/ yaml；四条准入正则实跑零命中，零 SQL/HTTP/LLM/模块级活状态。**通用装载器 src/soul/loader.ts 仍进不了 kernel**（模块级 new Set 单例 + 引 api 段写作语言判定），本文件不覆盖它那三个 api 段自管字段。请求回写 §4.7 见 docpatch RK-PSP" },
    { "path": "src/kernel/soul-codec.ts", "layer": "kernel", "basis": "【待定稿裁决】P4-11 新写：Soul 编解码三方法窄端口（parseValue / serialize / parseYaml），逐一对应人设生成角色的三处调用（JSON 结构校验 / 确定性序列化 / round-trip 自校验）。**加载器本身进不了 kernel**：src/soul/loader.ts 持模块级 new Set 单例，且 import 同目录的写作语言判定（api），进 kernel 即反向边。只 import kernel 的 soul-types.js 与 yaml.js。四条准入正则实跑零命中。消除 import 豁免 1 条（src/agents/persona-generator.ts -> src/soul/index.ts，content -> api）。请求回写 §4.7 见 docpatch RK-P4-11" },
    { "path": "src/kernel/publish-draft-contract.ts", "layer": "kernel", "basis": "【待定稿裁决】P4-5 新写：发布草稿 / 排期 / 稿件精修的跨属主契约——记录形状（ScheduledPublishRecord / ScheduledReconcileUpdate / DispatchDraft / EditDraft* / RefineDraft* / DraftRefinement 六型）+ 三个窄端口（ScheduledPublishStore 3 方法 / DraftRefinementDrafts 2 方法 / DraftRefinementReadWritePort 4 方法，逐字复刻消费方当下的 Pick<Store,…>）。两个属主文件（api 的 publish-log-store.ts、content 的 draft-refinement.ts）自建 pg 池、含建表 DDL，整文件进不了 kernel，故只析出契约段并在原位等值再导出（api 同层的面板 / 删图 / 审批四个消费方逐字无感）。三条 import 全是 kernel（platform-types / publish-pipeline-types / deployment-target）。四条准入正则实跑零命中。消除 import 豁免 4 条（publish-dispatcher / scheduled-publish-reconciler / draft-refinement-worker -> publish-log-store.ts，client-auth-server -> draft-refinement.ts）。请求回写 §4.7 见 docpatch RK-P4-5" },
    { "path": "src/kernel/note-detail.ts", "layer": "kernel", "basis": "【待定稿裁决·扩员】P5-B 抬入 NoteDetailData + NoteDetailImage（原 src/event-bus/types.ts，automation）。抬入理由不是共导多，而是**拆仓后 content 仓的 src 里没有 event-bus/types.ts**：concept-extractor-role / curated-note-evaluator 两个 content 角色以 NoteDetailData 为事件载荷类型（后者还用 NonNullable<NoteDetailData['images']>），留 automation 会让 content 仓模块解析失败、直接编译不过——豁免管得住门禁，管不住模块解析。NoteDetailImage 与协议 NoteImagePayload 逐字同形但**刻意不 import 它**（协议归 automation，kernel MUST NOT 反向依赖业务层），漂移由 automation 侧的赋值点 typecheck 兜住。原 NoteData 段不变。零 import、四条准入正则实跑零命中。消除 import 豁免 2 条。请求回写 §4.7 见 docpatch RK-P5-B" },
    { "path": "src/kernel/concept-pool.ts", "layer": "kernel", "basis": "【待定稿裁决】P5-B 新写：概念池纯数据模型 ConceptPool（原 src/event-bus/types.ts，automation）。抬入理由同 note-detail：属主是 content 的 concept-store.ts，留 automation 会让 content 仓编译不过。**同时删掉原 source: Map<string,string> 字段**——全仓只有写入点、零读取点，而拆仓后概念池由 content 装载、automation 的 role-dispatcher / search-evaluator 消费，跨进程要过 JSON，Map 序列化恒为 {}，读方永远拿到空表且无一处报错（红线：静默假成功）。关键词来源归属未丢，落在 concept_pool.source_note 列、经 ConceptRecord.sourceNote 读出，那条路径有真实消费者。零 import、四条准入正则实跑零命中。消除 import 豁免 1 条。请求回写 §4.7 见 docpatch RK-P5-B" },
    { "path": "src/kernel/feishu-card-contract.ts", "layer": "kernel", "basis": "【待定稿裁决·整文件移动】P5-3 从 src/comm/feishu-card-contract.ts（automation）git mv 抬入。该合同的生产方横跨 content（发布出口角色）与 automation（边云消息处理器 / 评论调度器）、渲染方在 api（feishu/cards.ts 的 buildXxxCard），三层共导；原落点归 automation 会让改判为 content 的发布出口角色为一组纯载荷类型跨服务边界。文件自述「纯类型、无任何 import」，四条准入正则实跑零命中。三个消费方已逐一 repoint，原路径不留再导出（原位再导出只是换马甲，不算消边）。消除 import 豁免 1 条。请求回写 §4.7 见 docpatch RK-P5-3" },
    { "path": "src/kernel/publish-platform-profile.ts", "layer": "kernel", "basis": "【待定稿裁决·部分析出】P5-3 从 src/publish-agent/platform-profile.ts（automation）析出档案本体：PublishImageSource / PublishTargetKind / PublishPlatformProfile / XHS_PUBLISH_PROFILE / FACEBOOK_PUBLISH_PROFILE / publishProfileForPlatform。**不是整文件移动**：原文件的 buildPublishCommandPlan 产出边云协议命令载荷（引 comm/protocol.ts），是货真价实的下发段代码、留 automation。直接诱因是改判为 content 的发布出口角色只为取一个 displayName 就引了整个下发段文件。唯一 import 是 kernel 自己的 platform-types（normalizePlatformId / PlatformId 本就定义在那里，platform/registry.ts 只是再导出）。两个常量是对象字面量、非 Map/Set，四条准入正则实跑零命中。请求回写 §4.6.3 见 docpatch RK-P5-3" },
    { "path": "src/kernel/risk-command-types.ts", "layer": "kernel", "basis": "【待定稿裁决】P5-1 新写（用户 2026-07-25 拍板：面板写风控走异步）：风控写命令的跨属主契约——受理回执 RiskCommandAccepted（**只有 commandId、无任何写后状态字段**）、四态结局 RiskCommandOutcome（processing / applied / failed / unknown）、两个提交入参型、以及只含「提交 + 回读」三方法的 RiskCommandPort。§4.6.4 明写 risk/status 与 risk/quota 两条端点 MUST 在拆分前迁走，否则 §14 红线 9「RiskController 单写」在拆分后即刻不成立；面板同步直调 RiskController 物理上跨不了进程。契约里刻意不含任何同步写方法：applySignal / setQuotaLevel / recoverRestricted 只活在 automation 的 RiskController 上。unknown 独立成态是硬要求——把「查无此命令」当成 processing，界面会永远转圈且永不报错（红线：静默假成功的另一形态）。零 import，四条准入正则实跑零命中。消除 import 豁免 1 条（src/panel/types.ts -> src/risk/index.ts，api -> automation，本 change 前该方向唯一一条边）。请求回写 §4.6.4 见 docpatch RK-P5-1" },
    { "path": "src/kernel/role-runtime.ts", "layer": "kernel", "basis": "【待定稿裁决·部分析出】P5-2 从 src/agents/base-role.ts（automation）析出其**无状态段**：resolveSoul / runRoleCompletion / roleLog / positiveTimeoutMs / oneLinePreview，外加 content 侧角色订阅的三条事件载荷 ContentRoleEventMap 与只含 on 的窄事件源 RoleEventSource。**不是整文件移动**：基类持事件总线句柄与人设快照，是带状态的行为类，撞「kernel 里不放行为类」。析出后 automation 的 BaseRole 与 content 的 ContentRole 都改成这些函数的薄壳，实现只有一份——复制第二份判定逻辑漂了没有任何机械手段会说话。RoleEventSource **刻意只给 on、不给 emit**：实测四个 content 角色全仓零 emit 命中；给出 emit 会让 content 在拆进程后能往一条它并不拥有的进程内总线上发事件，单体里跑得通、拆完静默失效。ContentRoleEventMap 重新声明而非 import（事件映射表归 automation，kernel MUST NOT 反向依赖业务层），漂移由 test/agents/content-role-events.test.ts 的编译期断言兜住。只 import kernel 的 soul-types / llm-contract / note-detail。四条准入正则实跑零命中。请求回写 §4.7 见 docpatch RK-P5-2" },
    { "path": "src/agents/content-role.ts", "layer": "automation", "basis": "§7.2.1 改判（用户 2026-07-29，change split-cloud-automation-production-runtime）：本类是那四个角色的公共基类，消费者恰好只有它们四个（全库实测），随它们改判 automation。P5-2 当初写下的『拆仓时随它们进 aidcp-content』已随 §7.2.1 作废——方向反了，但那句话背后的判据（基类跟着它服务的角色走）恰恰仍然成立，只是那四个角色的落点变了。迁移代价为零：本文件 import 全部指向 kernel（soul-types / llm-contract / role-runtime），换层不产生任何新的跨层边，与 §10.7 那条零代价机械依据同形。与 automation 属主的 base-role.ts 的两处收窄照旧（事件总线只收 RoleEventSource 即只订阅不发布；角色名裸 string），改判后两者同层但仍不合并——合并会让『只订阅不发布』这道收窄消失。原 basis 见 git history" },
    { "path": "src/kernel/panel-event-delivery-port.ts", "layer": "kernel", "basis": "【待定稿裁决】change split-cloud-api-composition-root-3b 新写：automation 的 panel.event replay 到 api 本地 fanout 的版本化投递合同（PanelEventDelivery / PanelEventDeliveryPort）与稳定 delivery id 纯函数。跨 automation/api 两域共导；零 import、零 SQL / HTTP / LLM、零进程内活状态。合同保留 execution_target 与 event_outbox:<target>:<id> 身份，使 HTTP 拒绝或回执丢失后可按同一 outbox id 至少一次重投，不承诺 exactly-once。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch。" },
    { "path": "src/kernel/api-direct-port.ts", "layer": "kernel", "basis": "change split-cloud-api-composition-root-4a 新写：3b 后 automation/API/content 三向 20 组 55 method slots 的纯 DTO、窄 port、version/target/error/token 方向常量与 source-derived inventory。只 import kernel 与 DeploymentTarget 类型；零 SQL、零 HTTP、零 SDK、零 owner 实现与模块级活状态。AccountOwnership 端口明确排除无 automation 消费者的 claimExecutionTarget，通知只暴露结构化 deliver，persona generation 不暴露通用 LLM。满足 §4.7 kernel 准入。" },
    { "path": "src/kernel/sync-read-snapshot.ts", "layer": "kernel", "basis": "【待定稿裁决】change split-cloud-api-composition-root-4b 新写：api/automation 同步读镜像共用的纯合同与实例状态机（闭集合 stream、shared/target scope、版本化 envelope、无符号 cursor、payload digest、atomic apply、freshness/readiness/health union，以及每进程可实例化的 config freshness runtime）。仅 import node:crypto、deployment-target 与同层 config-mirror 类型；无 SQL、HTTP 路由、LLM、模块级 Map/Set/定时器/连接池，不反向依赖业务层。HTTP adapter 留 src/transport/sync-read-snapshot-http.ts。满足 §4.7 kernel 准入。请求回写 §4.7 见 docpatch RK-4b-sync-read。" },
    { "path": "src/kernel/sync-read-facts.ts", "layer": "kernel", "basis": "change split-cloud-api-composition-root-4b 新写：A1-A6/B1-B5 闭集合 owner snapshot payload、source port 与 envelope 构造/校验。只 import 同层 sync-read contract、deployment target、platform/persona/config DTO；零 SQL、HTTP、LLM、模块级活状态，不反向依赖业务层。B1/B2/B4 只暴露 post-4a surviving fields。" },
    { "path": "src/kernel/llm-errors.ts", "layer": "kernel", "basis": "change split-cloud-automation-production-runtime 新写：LLM 错误族的唯一定义（ProviderKeyMissingError / LlmErrorMeta / buildLlmHttpError / buildLlmApiError / buildLlmShapeError + 其私有格式化闭包；formatLlmMeta 提升为 export 因留在 qwen.ts 的 LlmTimeoutError 也用它拼同一套排障字段，不导出就得复制第二份、字段位次会悄悄漂）。抬入理由是结构性的：拆仓后文本出口随 qwen.ts 进 aidcp-transport、视觉出口 vision.ts 留 content，两侧各持一份错误类会让跨副本 instanceof 恒 false，把『该厂商密钥缺失』静默降级成泛化的『模型不可用』（红线：静默假成功）。原处定义已删、非复制；vision.ts→qwen.ts 那条边随之彻底消失。零 import、四条准入正则实跑全 CLEAN、不 import 任何业务层文件。注：src/kernel/llm-contract.ts 文件头那句『错误类 …… 留 qwen.ts』已被本条推翻，已同批更正" },
    { "path": "src/kernel/content-port-error.ts", "layer": "kernel", "basis": "change split-cloud-automation-production-runtime 新写（取代已删的 content-port-result.ts 信封；用户 2026-07-29 裁定统一到本仓既有跨属主端口范式）：automation→content 两条端口共用的失败信号。端口本身回裸值、失败靠抛，与 publish-log-writer-port / CuratedContentReader 同范式；本文件只提供 6 选一具名 reason + 跨进程识别键 CONTENT_PORT_ERROR_NAME + 结构化守卫 isContentPortError（按 name + reason 两个自有可枚举属性判，**非 instanceof**——CLAUDE §8.5 跨进程后 instanceof 恒 false）。另带线上 code content_port_<reason> 与还原函数 contentPortReasonFromCode：本仓内部 HTTP 传输的 encodeHandlerError 只保 code + message，name / reason 跨那一跳会全丢；**还原不出返回 null，MUST NOT 套默认 reason**，否则 unsupported_method 会被吞成 remote_error、概念池的回落分支再次变成死代码。零 import、零 SQL/HTTP/LLM/模块级活状态" },
    { "path": "src/kernel/concept-pool-port.ts", "layer": "kernel", "basis": "change split-cloud-automation-production-runtime 新写：概念池跨属主端口面（表 concepts 属 content）。6 个方法逐条标注真实调用点；签名照抄属主 ConceptStore 真实签名，故属主实例可被组装根原样注入。**口径更正**：addCandidate 在 automation 仓里只有接口声明、没有直接调用点——它是透传给 content 属主的概念抽取角色工厂的，所以「消费方全在 automation」对这一条不成立。两个读方法**都不带可选标记**，回落改由具名的 unsupported_method 驱动——今天的 typeof 能力探针跨进程后恒为真（客户端类总是定义着方法），保留可选等于保留一张假的安全网、真实能力缺口反被静默吞掉。诚实要求不变、载体变了：addCandidate 回 boolean（照抄属主），MUST NOT 因为请求发出去了就报成功。失败抛 ContentPortError，**绝不回空集合**。仅 import kernel；零 SQL/HTTP/LLM/模块级活状态" },
    { "path": "src/kernel/text-card-transcriber-port.ts", "layer": "kernel", "basis": "change split-cloud-automation-production-runtime 新写：图内文字转写的调用口与能力状态（TextCardTranscriber / Input / Outcome + TextCardTranscriberCapability 二态 + TextCardTranscriptionMode 三态）。这条缝的两侧分居两仓——转写本体按 §7.2.1 留 content（它依赖的封面形态感知是真·双段共用，组装根在两段各建一个实例），调用方 curated_note_evaluator 改判 automation——所以形状必须只有一份定义。能力二态的存在理由是红线：原写法 this.textCardTranscriber?.enabled() 在依赖缺席时表达式为 undefined、判定为假，于是『旗标关掉了』与『依赖没接上』长得一模一样，调用方照拿成功。零 IO，只 import kernel/curated-content-types.js；四条准入正则实跑全 CLEAN" },
    { "path": "src/kernel/curated-selection-port.ts", "layer": "kernel", "basis": "change split-cloud-automation-production-runtime 新写：精选库召回的跨属主端口面（表 curated_content 属 content，两个消费方是 automation 的发帖与评论调度器）。刻意是两个方法而非一个：发帖侧要全字段，评论侧只要三字段窄投影——全字段视图挂着参照图集/视觉分析/文字卡转写等大块 JSON，搬过进程边界只为留三个字段，投影本就该在属主侧做。缺失计数用 null 而非 0，区别『不知道』与『真是 0』。失败抛 ContentPortError、**绝不回空数组**——回空数组正是今天五处降级形状把『连不上对面』吃成『没有精选素材』的那个形态。仅 import kernel；零 SQL/HTTP/LLM/模块级活状态" },
    { "path": "src/kernel/curated-write-port.ts", "layer": "kernel", "basis": "change split-cloud-automation-production-runtime 新写：精选库写侧的跨属主端口面（表 curated_content 属 content；台账条目 content-curated-write-authority）。与 curated-selection-port 是同一张表的两张脸、刻意分文件：召回那张的消费方是发帖与评论调度器，本张是两个精选准入评估角色 + 组装根，失败后果也不同（召回失败＝没素材可用，写失败＝这一条观测永久丢了）；合成一个端口会让只需要读的调用方结构上也拿到写能力。只开 5 个方法，逐条对得上真实调用点：upsertObservation（curated-note-evaluator.ts:362）、refreshReferenceImages（同文件:253）、getTextCardContext（同文件:293）、archiveComment（curated-comment-evaluator.ts:159）、markBotAction（组装根 segC 的自有点赞/收藏并入语料）。那两个角色类已随 task 0.7 改判 automation 属主，所以这是货真价实的跨属主写。getTextCardContext 是本张脸上唯一的读，放这里而非召回那张：它只被准入评估在写之前当读穿缓存用，与按账号挑素材、带排序与时间窗的召回既不同调用方也不同语义。签名照抄属主真实签名，属主实例结构上即满足。三条只对写侧成立的 MUST：返回 void 的三个方法跨线时由传输层回显式回执（undefined 编码后是空响应体，与『路由压根没跑』逐字节一样，写没做成会读起来像做成了）；写失败 MUST NOT 被译成『这条本来就不该写』（属主侧几处真实 no-op 与写失败在单体里靠抛不抛区分得开，跨进程后一旦有哪层把抛出吃成静默返回，两件事永久混成一件，而精选语料只会少不会多、少一条谁都不会发现）；refreshReferenceImages 回真实受影响行数、MUST NOT 编造（0 意味着库里没有这条源帖，回乐观的 1 会让后续转写往一条不存在的行上写）。仅 import kernel 类型；零 SQL/HTTP/LLM/模块级活状态" },
    { "path": "src/kernel/facebook-publish-media-port.ts", "layer": "kernel", "basis": "change split-cloud-automation-production-runtime 新写：FB 发帖素材状态流转的跨属主端口面（两张表 account_facebook_publish_image_set / account_facebook_publish_image 属 content、无写豁免；台账条目 content-facebook-publish-media-authority）。只开 3 个方法 releaseReservation / markUsed / quarantine——它们是 automation 侧的全部消费面（publish-dispatcher.ts:545/547/549，外加 server.ts:6146 那处不走窄口的组装根直调），签名逐字等于 publish-dispatcher.ts:134-138 已有的窄口，是把它抬进 kernel 而非新造一套。属主另外 6 个方法一个都不开：list/upload/reorder/updateSet 与 availableCount 的消费方文件归 api（后者最易判错——装配点在组装根 automation 段 server.ts:7316，但消费方 content-scheduler.ts 归属是 api；判据是消费方文件归属，不是它在哪一段被 new），reserveNext 归 content 自己，deleteSet/close 零调用点。boolean 回执的语义是「那一行真的被改了吗」，读不到 MUST 抛、MUST NOT 落成 false——false 会被读成「素材早被别人动过」，而真相是这次记账没发生、素材永久卡在保留态没人回收。失败信号复用 ContentPortError，刻意不在这条边上立第二个 name：属主那族 FacebookPublishMediaError 的具名 reason 原样进 detail 供定位、不参与判定（今天 automation 两个调用点都不按 reason 分支）。零 import、零 SQL/HTTP/LLM/模块级活状态" },
    { "path": "src/kernel/llm-usage-recording-port.ts", "layer": "kernel", "basis": "change split-cloud-automation-production-runtime 新写：模型 token 用量记账写侧的跨属主端口面（表 llm_token_usage 属 content，属主 src/metrics/token-usage-store.ts 自称唯一写入口；台账条目 content-token-usage-authority）。只开 1 个方法 recordUsage——automation 侧唯一消费点是 server.ts:6507（segCAutomation 段内文字卡 OCR 视觉调用的用量上报闭包）；upsertBillingPrices / billingPriceTargets 的消费方是 api（面板手动账单价格刷新）、purgeOlderThan 属主自驱、读侧 usage() 亦 api，一个都不开。这是唯一一处刻意不照抄属主签名的端口：属主入口 add(info):void 同步且纯内存缓冲，跨进程照抄只会得到一个必然撒谎的方法（void 无处报失败＝静默假成功；一次模型调用一次 HTTP 又打掉属主自己列的「批量」不变量、把低频旁路挂上模型调用热路径），故改成批量提交已合并的增量行——正是属主那个定时器今天真正落库的东西。属主今天没有这个方法，与 curated-selection-port 的 selectSamplesForSearchTerms 同形：content 接线时补方法或交适配对象，否则服务端在场探针答具名 unsupported_method、绝不静默变成「记上了」。三条 MUST：桶起点由调用方戳（属主重算会把一批用量整体挪进错误的时间桶）、传输失败 MUST NOT 重投（ON CONFLICT 累加，重投即翻倍；超时按属主「用量可丢」约定丢弃并计数留痕）、回执是真正落库的行数而非「请求发出去了」。本端口 MUST NOT 出现任何单价 / 金额 / 币种字段：成本 MUST 由厂商账单反算（已上线规格 llm-token-usage-stats 的 Token Usage Cost Estimates），禁止在这一层硬编码价目表。零 import、零 SQL/HTTP/LLM/模块级活状态" },
    { "path": "src/kernel/operator-command-port.ts", "layer": "kernel", "basis": "change split-cloud-automation-production-runtime 新写：四条运营指令（自由文本委托 / 发布评论指令 / 委托任务卡片动作 / 调度启停）的跨属主窄口，共同形状为『入站在 api、处理器在 automation』。形状逐字复用 4a 已建立的 paired command（api-direct-port.ts 的信封 + 幂等键 + applied|duplicate|collision 回执），不另造第二套机制；executionTarget 由客户端从本进程部署事实注入、调用方无入口可选，DEV/OL 共库下这条是硬约束。四条语义钉在类型上：不可达回 not_delivered 具名原因（MUST NOT 说成已受理）、传输失败/超时抛出且原因码明说『结果未知』（发布/评论/启停都有真实副作用，绝不推断成功或失败）、重放按持久台账原样回放首次结果（MUST NOT 现算一个 changed:false）。不新造错误类，只补 isDelegatedTaskServiceError 结构化守卫——今天两处消费方（飞书 delegated-task-card 判 version_conflict、client-auth-server 映射 HTTP 状态）用 instanceof 认它，跨进程后恒 false。仅 import 同层 kernel 契约与 DeploymentTarget；零 SQL/HTTP/LLM/模块级活状态" },
    { "path": "src/kernel/content-scheduling-port.ts", "layer": "kernel", "basis": "change wire-content-scheduler-into-api-process 新写：内容排期调度器向 automation 取用的那一族窄口（在线账号清单 / 风控状态 / 三个『在不在跑』/ 委托任务占用 / 三类计数上限 / 三类扳机），方向为 api→automation——排期器类归 api（src/orchestrator/content-scheduler.ts），它每分钟要问的事实与真正的扳机住在 automation。形状逐字复用 4a 的信封（api-direct-port.ts 的 version + executionTarget + input），executionTarget 由客户端从本机部署事实注入、调用方无入口可选：DEV/OL 共库下这条是硬约束。本端口存在的首要理由是把『问不到时算什么』写进契约并按『哪边更严』定死：在线清单问不到 MUST 整轮跳过（回空数组与真的没人在线在外部完全同形，全员静默停摆且看着正常）、风控状态问不到 MUST 跳过该账号（回 normal 就是放行）、三个是否在跑与委托占用问不到 MUST 判为在跑/占用（回不忙是放行，失败开闸即双发）。故所有方法一律返回裸值、失败靠抛，MUST NOT 在契约里出现任何『问不到时的缺省值』形状——缺省值一进契约，判更严的那个决定就从调用方手里被悄悄拿走了。三条扳机的回执语义是受不受理（accepted + 具名 reason + retryable），MUST NOT 挂到生成结束：一次 HTTP 挂住整条生成链会把超时语义搅进业务结局；终态结果卡由持有结局的 automation 侧发，调度器只对未受理回卡。仅 import 同层 kernel 契约与 DeploymentTarget；零 SQL/HTTP/LLM/模块级活状态" },
    { "path": "src/kernel/scheduled-automation-catalog.ts", "layer": "kernel", "basis": "change split-cloud-api-composition-root-4b 从 automation registry 析出的静态 scheduled-automation catalog 与纯 reader。只 import 同层 platform types；零 SQL、HTTP、LLM、模块级可变活状态。automation registry 保留等值再导出以维持现有调用。" },
    { "path": "src/config/api-sync-read-source.ts", "layer": "api", "basis": "change split-cloud-api-composition-root-4b：API owner 的 B1/B2/B4/B5 repeatable-read snapshot provider，读取 API owner 数据库并与 config_mirror_version 同事务；含 SQL，明确不进 kernel。" },
    { "path": "src/config/api-sync-read-mirrors.ts", "layer": "api", "basis": "change split-cloud-api-composition-root-4b：API consumer 的 A1/A3/A4/A5/A6 进程内 last-good 镜像与 readiness adapter；只消费 kernel contract，不持 owner 权威。"}
  ]
}
